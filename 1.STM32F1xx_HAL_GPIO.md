# STM32F1xx HAL GPIO库函数说明文档

本文档介绍了STM32F1xx HAL库中的GPIO函数，包括函数原型、参数、返回值和具体作用。

## 1. GPIO __HAL 宏函数

### __HAL_GPIO_EXTI_GET_FLAG

**宏原型**：

```c
#define __HAL_GPIO_EXTI_GET_FLAG(__EXTI_LINE__) (EXTI->PR & (__EXTI_LINE__))
```

**参数**：

- `__EXTI_LINE__`：要检查的EXTI线标志，可以是GPIO_PIN_x，其中x可以是(0..15)

**返回值**：

- 返回EXTI线的标志状态（SET或RESET）

**作用**：检查指定的EXTI线标志是否被置位

### __HAL_GPIO_EXTI_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_GPIO_EXTI_CLEAR_FLAG(__EXTI_LINE__) (EXTI->PR = (__EXTI_LINE__))
```

**参数**：

- `__EXTI_LINE__`：要清除的EXTI线标志，可以是GPIO_PIN_x的组合，其中x可以是(0..15)

**返回值**：无

**作用**：清除EXTI的线路挂起标志

### __HAL_GPIO_EXTI_GET_IT

**宏原型**：

```c
#define __HAL_GPIO_EXTI_GET_IT(__EXTI_LINE__) (EXTI->PR & (__EXTI_LINE__))
```

**参数**：

- `__EXTI_LINE__`：要检查的EXTI线，可以是GPIO_PIN_x，其中x可以是(0..15)

**返回值**：

- 返回EXTI线的状态（SET或RESET）

**作用**：检查指定的EXTI线是否被断言

### __HAL_GPIO_EXTI_CLEAR_IT

**宏原型**：

```c
#define __HAL_GPIO_EXTI_CLEAR_IT(__EXTI_LINE__) (EXTI->PR = (__EXTI_LINE__))
```

**参数**：

- `__EXTI_LINE__`：要清除的EXTI线，可以是GPIO_PIN_x的组合，其中x可以是(0..15)

**返回值**：无

**作用**：清除EXTI的线路挂起位

### __HAL_GPIO_EXTI_GENERATE_SWIT

**宏原型**：

```c
#define __HAL_GPIO_EXTI_GENERATE_SWIT(__EXTI_LINE__) (EXTI->SWIER |= (__EXTI_LINE__))
```

**参数**：

- `__EXTI_LINE__`：要生成软件中断的EXTI线，可以是GPIO_PIN_x，其中x可以是(0..15)

**返回值**：无

**作用**：在选定的EXTI线上生成软件中断 

## 2. GPIO初始化函数

### HAL_GPIO_Init

**函数原型**：
```c
void HAL_GPIO_Init(GPIO_TypeDef  *GPIOx, GPIO_InitTypeDef *GPIO_Init);
```

**参数**：
- `GPIOx`：GPIO端口，其中x可以是(A..G)，取决于设备
- `GPIO_Init`：指向GPIO_InitTypeDef结构体的指针，包含GPIO配置信息

**返回值**：无

**作用**：根据GPIO_Init中指定的参数初始化GPIOx外设

### HAL_GPIO_DeInit

**函数原型**：
```c
void HAL_GPIO_DeInit(GPIO_TypeDef  *GPIOx, uint32_t GPIO_Pin);
```

**参数**：
- `GPIOx`：GPIO端口，其中x可以是(A..G)，取决于设备
- `GPIO_Pin`：要配置的端口位，可以是GPIO_PIN_x的组合，其中x可以是(0..15)

**返回值**：无

**作用**：将指定的GPIO端口重置为默认状态

## 3. GPIO操作函数

### HAL_GPIO_ReadPin

**函数原型**：
```c
GPIO_PinState HAL_GPIO_ReadPin(GPIO_TypeDef *GPIOx, uint16_t GPIO_Pin);
```

**参数**：
- `GPIOx`：GPIO端口，其中x可以是(A..G)，取决于设备
- `GPIO_Pin`：要读取的端口位，可以是GPIO_PIN_x，其中x可以是(0..15)

**返回值**：
- `GPIO_PIN_SET`：如果引脚状态为高电平
- `GPIO_PIN_RESET`：如果引脚状态为低电平

**作用**：读取指定GPIO引脚的当前状态

### HAL_GPIO_WritePin

**函数原型**：
```c
void HAL_GPIO_WritePin(GPIO_TypeDef *GPIOx, uint16_t GPIO_Pin, GPIO_PinState PinState);
```

**参数**：
- `GPIOx`：GPIO端口，其中x可以是(A..G)，取决于设备
- `GPIO_Pin`：要写入的端口位，可以是GPIO_PIN_x，其中x可以是(0..15)
- `PinState`：要写入的值，可以是GPIO_PIN_SET或GPIO_PIN_RESET

**返回值**：无

**作用**：设置或清除指定的GPIO引脚

### HAL_GPIO_TogglePin

**函数原型**：
```c
void HAL_GPIO_TogglePin(GPIO_TypeDef *GPIOx, uint16_t GPIO_Pin);
```

**参数**：
- `GPIOx`：GPIO端口，其中x可以是(A..G)，取决于设备
- `GPIO_Pin`：要切换的端口位，可以是GPIO_PIN_x，其中x可以是(0..15)

**返回值**：无

**作用**：切换指定GPIO引脚的状态（高变低，低变高）

### HAL_GPIO_LockPin

**函数原型**：

```c
HAL_StatusTypeDef HAL_GPIO_LockPin(GPIO_TypeDef *GPIOx, uint16_t GPIO_Pin);
```

**参数**：

- `GPIOx`：GPIO端口，其中x可以是(A..G)，取决于设备
- `GPIO_Pin`：要锁定的端口位，可以是GPIO_PIN_x的组合，其中x可以是(0..15)

**返回值**：

- `HAL_OK`：锁定操作成功
- `HAL_ERROR`：锁定操作失败

**作用**：锁定GPIO引脚配置，锁定后直到下一次复位前无法修改

## 4. GPIO中断处理函数

### HAL_GPIO_EXTI_IRQHandler

**函数原型**：
```c
void HAL_GPIO_EXTI_IRQHandler(uint16_t GPIO_Pin);
```

**参数**：
- `GPIO_Pin`：与EXTI线路连接的引脚

**返回值**：无

**作用**：处理EXTI中断请求

## 5. GPIO回调函数

### HAL_GPIO_EXTI_Callback

**函数原型**：
```c
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin);
```

**参数**：
- `GPIO_Pin`：与EXTI线路连接的引脚

**返回值**：无

**作用**：EXTI线路检测回调函数，在HAL_GPIO_EXTI_IRQHandler中被调用

