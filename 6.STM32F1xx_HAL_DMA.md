# STM32F1xx HAL DMA库函数说明文档

本文档介绍了STM32F1xx HAL库中的DMA函数，包括函数原型、参数、返回值和具体作用。

## 1. DMA __HAL 宏函数

### __HAL_DMA_RESET_HANDLE_STATE

**宏原型**：

```c
#define __HAL_DMA_RESET_HANDLE_STATE(__HANDLE__) ((__HANDLE__)->State = HAL_DMA_STATE_RESET)
```

**参数**：

- `__HANDLE__`：DMA句柄

**返回值**：无

**作用**：重置DMA句柄状态

### __HAL_DMA_ENABLE

**宏原型**：

```c
#define __HAL_DMA_ENABLE(__HANDLE__)        (SET_BIT((__HANDLE__)->Instance->CCR, DMA_CCR_EN))
```

**参数**：

- `__HANDLE__`：DMA句柄

**返回值**：无

**作用**：使能指定的DMA通道

### __HAL_DMA_DISABLE

**宏原型**：

```c
#define __HAL_DMA_DISABLE(__HANDLE__)       (CLEAR_BIT((__HANDLE__)->Instance->CCR, DMA_CCR_EN))
```

**参数**：

- `__HANDLE__`：DMA句柄

**返回值**：无

**作用**：禁用指定的DMA通道

### __HAL_DMA_ENABLE_IT

**宏原型**：

```c
#define __HAL_DMA_ENABLE_IT(__HANDLE__, __INTERRUPT__)   (SET_BIT((__HANDLE__)->Instance->CCR, (__INTERRUPT__)))
```

**参数**：

- `__HANDLE__`：DMA句柄
- `__INTERRUPT__`：指定要使能的DMA中断源，可以是以下值的组合：
  - `DMA_IT_TC`：传输完成中断掩码
  - `DMA_IT_HT`：半传输完成中断掩码
  - `DMA_IT_TE`：传输错误中断掩码

**返回值**：无

**作用**：使能指定的DMA通道中断

### __HAL_DMA_DISABLE_IT

**宏原型**：

```c
#define __HAL_DMA_DISABLE_IT(__HANDLE__, __INTERRUPT__)  (CLEAR_BIT((__HANDLE__)->Instance->CCR , (__INTERRUPT__)))
```

**参数**：

- `__HANDLE__`：DMA句柄
- `__INTERRUPT__`：指定要禁用的DMA中断源，可以是以下值的组合：
  - `DMA_IT_TC`：传输完成中断掩码
  - `DMA_IT_HT`：半传输完成中断掩码
  - `DMA_IT_TE`：传输错误中断掩码

**返回值**：无

**作用**：禁用指定的DMA通道中断

### __HAL_DMA_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_DMA_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__)  ((((__HANDLE__)->Instance->CCR & (__INTERRUPT__)) == (__INTERRUPT__)) ? SET : RESET)
```

**参数**：

- `__HANDLE__`：DMA句柄
- `__INTERRUPT__`：指定要检查的DMA中断源，可以是以下值之一：
  - `DMA_IT_TC`：传输完成中断掩码
  - `DMA_IT_HT`：半传输完成中断掩码
  - `DMA_IT_TE`：传输错误中断掩码

**返回值**：

- 返回DMA中断源的状态（SET或RESET）

**作用**：检查指定的DMA中断源是否使能

### __HAL_DMA_GET_COUNTER

**宏原型**：

```c
#define __HAL_DMA_GET_COUNTER(__HANDLE__) ((__HANDLE__)->Instance->CNDTR)
```

**参数**：

- `__HANDLE__`：DMA句柄

**返回值**：

- 返回当前DMA通道传输中剩余的数据单元数

**作用**：获取DMA当前传输剩余的数据单元数

## 2. DMA初始化函数

### HAL_DMA_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_DMA_Init(DMA_HandleTypeDef *hdma);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针，包含DMA通道的配置信息

**返回值**：
- `HAL_OK`：初始化成功
- `HAL_ERROR`：初始化失败

**作用**：根据DMA_InitTypeDef结构体中指定的参数初始化DMA通道

### HAL_DMA_DeInit

**函数原型**：
```c
HAL_StatusTypeDef HAL_DMA_DeInit(DMA_HandleTypeDef *hdma);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针，包含DMA通道的配置信息

**返回值**：
- `HAL_OK`：反初始化成功
- `HAL_ERROR`：反初始化失败

**作用**：将指定的DMA通道重置为默认状态

## 3. DMA启动函数

### HAL_DMA_Start

**函数原型**：
```c
HAL_StatusTypeDef HAL_DMA_Start(DMA_HandleTypeDef *hdma, uint32_t SrcAddress, uint32_t DstAddress, uint32_t DataLength);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针
- `SrcAddress`：源内存缓冲区地址
- `DstAddress`：目标内存缓冲区地址
- `DataLength`：要从源传输到目标的数据长度

**返回值**：
- `HAL_OK`：传输启动成功
- `HAL_BUSY`：DMA通道正忙于其他传输

**作用**：开始DMA传输

### HAL_DMA_Abort

**函数原型**：

```c
HAL_StatusTypeDef HAL_DMA_Abort(DMA_HandleTypeDef *hdma);
```

**参数**：

- `hdma`：指向DMA_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：传输终止成功
- `HAL_ERROR`：没有正在进行的传输

**作用**：终止当前的DMA传输

### HAL_DMA_Start_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_DMA_Start_IT(DMA_HandleTypeDef *hdma, uint32_t SrcAddress, uint32_t DstAddress, uint32_t DataLength);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针
- `SrcAddress`：源内存缓冲区地址
- `DstAddress`：目标内存缓冲区地址
- `DataLength`：要从源传输到目标的数据长度

**返回值**：
- `HAL_OK`：传输启动成功
- `HAL_BUSY`：DMA通道正忙于其他传输

**作用**：以中断模式开始DMA传输



### HAL_DMA_Abort_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_DMA_Abort_IT(DMA_HandleTypeDef *hdma);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：传输终止成功
- `HAL_ERROR`：没有正在进行的传输

**作用**：在中断模式下终止DMA传输

## 4. DMA操作函数

### HAL_DMA_PollForTransfer

**函数原型**：
```c
HAL_StatusTypeDef HAL_DMA_PollForTransfer(DMA_HandleTypeDef *hdma, uint32_t CompleteLevel, uint32_t Timeout);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针
- `CompleteLevel`：指定DMA完成级别，可以是以下值之一：
  - `HAL_DMA_FULL_TRANSFER`：完全传输
  - `HAL_DMA_HALF_TRANSFER`：半传输
- `Timeout`：超时时间

**返回值**：
- `HAL_OK`：轮询成功
- `HAL_ERROR`：传输错误或无传输
- `HAL_TIMEOUT`：轮询超时

**作用**：轮询传输完成标志

## 5. DMA状态函数

### HAL_DMA_GetState

**函数原型**：
```c
HAL_DMA_StateTypeDef HAL_DMA_GetState(DMA_HandleTypeDef *hdma);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针

**返回值**：
- 返回DMA句柄状态，可以是以下值之一：
  - `HAL_DMA_STATE_RESET`：DMA未初始化或已禁用
  - `HAL_DMA_STATE_READY`：DMA已初始化并准备使用
  - `HAL_DMA_STATE_BUSY`：DMA正在进行
  - `HAL_DMA_STATE_TIMEOUT`：DMA超时状态

**作用**：获取DMA句柄状态

### HAL_DMA_GetError

**函数原型**：
```c
uint32_t HAL_DMA_GetError(DMA_HandleTypeDef *hdma);
```

**参数**：
- `hdma`：指向DMA_HandleTypeDef结构体的指针

**返回值**：
- 返回DMA错误代码，可以是以下值之一：
  - `HAL_DMA_ERROR_NONE`：无错误
  - `HAL_DMA_ERROR_TE`：传输错误
  - `HAL_DMA_ERROR_NO_XFER`：没有正在进行的传输
  - `HAL_DMA_ERROR_TIMEOUT`：超时错误
  - `HAL_DMA_ERROR_NOT_SUPPORTED`：不支持的模式

**作用**：获取DMA错误代码 

## 6. DMA中断处理函数

### HAL_DMA_IRQHandler

**函数原型**：

```c
void HAL_DMA_IRQHandler(DMA_HandleTypeDef *hdma);
```

**参数**：

- `hdma`：指向DMA_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理DMA中断请求

## 7. DMA回调注册/注销函数

### HAL_DMA_RegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_DMA_RegisterCallback(DMA_HandleTypeDef *hdma, HAL_DMA_CallbackIDTypeDef CallbackID, void (* pCallback)(DMA_HandleTypeDef *_hdma));
```

**参数**：

- `hdma`：指向DMA_HandleTypeDef结构体的指针
- `CallbackID`：用户回调标识符，HAL_DMA_CallbackIDTypeDef枚举类型，可以是：
  - `HAL_DMA_XFER_CPLT_CB_ID`：传输完成回调
  - `HAL_DMA_XFER_HALFCPLT_CB_ID`：半传输完成回调
  - `HAL_DMA_XFER_ERROR_CB_ID`：错误回调
  - `HAL_DMA_XFER_ABORT_CB_ID`：终止回调
- `pCallback`：指向私有回调函数的指针，该函数以DMA_HandleTypeDef结构体作为参数

**返回值**：

- `HAL_OK`：回调注册成功
- `HAL_ERROR`：回调注册失败

**作用**：注册用户回调函数

### HAL_DMA_UnRegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_DMA_UnRegisterCallback(DMA_HandleTypeDef *hdma, HAL_DMA_CallbackIDTypeDef CallbackID);
```

**参数**：

- `hdma`：指向DMA_HandleTypeDef结构体的指针
- `CallbackID`：用户回调标识符，HAL_DMA_CallbackIDTypeDef枚举类型，可以是：
  - `HAL_DMA_XFER_CPLT_CB_ID`：传输完成回调
  - `HAL_DMA_XFER_HALFCPLT_CB_ID`：半传输完成回调
  - `HAL_DMA_XFER_ERROR_CB_ID`：错误回调
  - `HAL_DMA_XFER_ABORT_CB_ID`：终止回调
  - `HAL_DMA_XFER_ALL_CB_ID`：所有回调

**返回值**：

- `HAL_OK`：回调注销成功
- `HAL_ERROR`：回调注销失败

**作用**：注销用户回调函数