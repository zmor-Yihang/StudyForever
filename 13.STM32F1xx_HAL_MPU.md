# STM32 MPU库函数说明文档

本文档介绍了STM32 HAL库中的MPU(内存保护单元)函数，包括函数原型、参数、返回值和具体作用。

## 1. MPU __HAL 宏函数

### __HAL_MPU_ENABLE

**宏原型**：

```c
#define __HAL_MPU_ENABLE(MPU_Control) do {                                                   \
                                            MODIFY_REG(MPU->CTRL, MPU_CTRL_PRIVDEFENA_Msk | MPU_CTRL_HFNMIENA_Msk, MPU_Control); \
                                            __DSB();                                          \
                                            __ISB();                                          \
                                          } while(0)
```

**参数**：

- `MPU_Control`：MPU控制参数，可以是以下值的组合:
  - `MPU_HFNMI_PRIVDEF_NONE`：特权状态下的访问不启用背景区域
  - `MPU_HARDFAULT_NMI`：硬件故障和NMI处理器使用MPU
  - `MPU_PRIVILEGED_DEFAULT`：特权状态下的访问启用背景区域

**返回值**：无

**作用**：启用MPU功能并设置控制参数

### __HAL_MPU_DISABLE

**宏原型**：

```c
#define __HAL_MPU_DISABLE() do {                                    \
                                   CLEAR_BIT(MPU->CTRL, MPU_CTRL_ENABLE_Msk);   \
                                   __DSB();                                      \
                                   __ISB();                                      \
                                 } while(0)
```

**参数**：无

**返回值**：无

**作用**：禁用MPU功能

### __HAL_MPU_BACKGROUND_REGION_ENABLE

**宏原型**：

```c
#define __HAL_MPU_BACKGROUND_REGION_ENABLE() SET_BIT(MPU->CTRL, MPU_CTRL_PRIVDEFENA_Msk)
```

**参数**：无

**返回值**：无

**作用**：启用背景区域（在MPU未设置区域时使用默认内存映射）

### __HAL_MPU_BACKGROUND_REGION_DISABLE

**宏原型**：

```c
#define __HAL_MPU_BACKGROUND_REGION_DISABLE() CLEAR_BIT(MPU->CTRL, MPU_CTRL_PRIVDEFENA_Msk)
```

**参数**：无

**返回值**：无

**作用**：禁用背景区域

## 2. MPU初始化和配置函数

### HAL_MPU_Enable

**函数原型**：
```c
void HAL_MPU_Enable(uint32_t MPU_Control);
```

**参数**：
- `MPU_Control`：MPU控制参数，可以是以下值的组合:
  - `MPU_HFNMI_PRIVDEF_NONE`：特权状态下的访问不启用背景区域
  - `MPU_HARDFAULT_NMI`：硬件故障和NMI处理器使用MPU
  - `MPU_PRIVILEGED_DEFAULT`：特权状态下的访问启用背景区域

**返回值**：无

**作用**：启用MPU并设置控制参数

### HAL_MPU_Disable

**函数原型**：
```c
void HAL_MPU_Disable(void);
```

**参数**：无

**返回值**：无

**作用**：禁用MPU功能

### HAL_MPU_ConfigRegion

**函数原型**：
```c
void HAL_MPU_ConfigRegion(MPU_Region_InitTypeDef *MPU_Init);
```

**参数**：
- `MPU_Init`：指向MPU_Region_InitTypeDef结构体的指针，包含MPU区域配置信息

**返回值**：无

**作用**：配置MPU内存区域的属性

## 3. MPU区域属性设置函数

### HAL_MPU_ConfigMemoryAttributes

**函数原型**：
```c
void HAL_MPU_ConfigMemoryAttributes(MPU_Attributes_InitTypeDef *MPU_AttributesInit);
```

**参数**：
- `MPU_AttributesInit`：指向MPU_Attributes_InitTypeDef结构体的指针，包含内存属性配置信息

**返回值**：无

**作用**：配置MPU内存属性

## 4. MPU错误处理函数

### HAL_MPU_ErrorCallback

**函数原型**：
```c
void HAL_MPU_ErrorCallback(void);
```

**参数**：无

**返回值**：无

**作用**：MPU错误回调函数，由用户实现以处理内存保护违规

### HAL_MPU_ConfigRegion_MemMap

**函数原型**：
```c
HAL_StatusTypeDef HAL_MPU_ConfigRegion_MemMap(MPU_MemMap_Region_InitTypeDef *MPU_MemMap_Init);
```

**参数**：
- `MPU_MemMap_Init`：指向MPU_MemMap_Region_InitTypeDef结构体的指针，包含内存映射区域配置

**返回值**：
- `HAL_OK`：配置成功
- `HAL_ERROR`：配置失败

**作用**：使用内存映射方式配置MPU区域
