# STM32F1xx HAL I2C库函数说明文档

本文档介绍了STM32F1xx HAL库中的I2C函数，包括函数原型、参数、返回值和具体作用。

## 1. I2C __HAL 宏函数

### __HAL_I2C_RESET_HANDLE_STATE

**宏原型**：

```c
#if (USE_HAL_I2C_REGISTER_CALLBACKS == 1)
#define __HAL_I2C_RESET_HANDLE_STATE(__HANDLE__)                do{                                                   \
                                                                    (__HANDLE__)->State = HAL_I2C_STATE_RESET;       \
                                                                    (__HANDLE__)->MspInitCallback = NULL;            \
                                                                    (__HANDLE__)->MspDeInitCallback = NULL;          \
                                                                  } while(0)
#else
#define __HAL_I2C_RESET_HANDLE_STATE(__HANDLE__)                ((__HANDLE__)->State = HAL_I2C_STATE_RESET)
#endif
```

**参数**：

- `__HANDLE__`：I2C句柄

**返回值**：无

**作用**：重置I2C句柄状态

### __HAL_I2C_ENABLE_IT

**宏原型**：

```c
#define __HAL_I2C_ENABLE_IT(__HANDLE__, __INTERRUPT__)   SET_BIT((__HANDLE__)->Instance->CR2,(__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：I2C句柄
- `__INTERRUPT__`：要使能的中断，可以是以下值之一：
  - I2C_IT_BUF: 缓冲区中断使能
  - I2C_IT_EVT: 事件中断使能
  - I2C_IT_ERR: 错误中断使能

**返回值**：无

**作用**：使能指定的I2C中断

### __HAL_I2C_DISABLE_IT

**宏原型**：

```c
#define __HAL_I2C_DISABLE_IT(__HANDLE__, __INTERRUPT__)  CLEAR_BIT((__HANDLE__)->Instance->CR2, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：I2C句柄
- `__INTERRUPT__`：要禁用的中断，可以是以下值之一：
  - I2C_IT_BUF: 缓冲区中断使能
  - I2C_IT_EVT: 事件中断使能
  - I2C_IT_ERR: 错误中断使能

**返回值**：无

**作用**：禁用指定的I2C中断

### __HAL_I2C_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_I2C_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) ((((__HANDLE__)->Instance->CR2 & (__INTERRUPT__)) == (__INTERRUPT__)) ? SET : RESET)
```

**参数**：

- `__HANDLE__`：I2C句柄
- `__INTERRUPT__`：要检查的I2C中断源，可以是以下值之一：
  - I2C_IT_BUF: 缓冲区中断使能
  - I2C_IT_EVT: 事件中断使能
  - I2C_IT_ERR: 错误中断使能

**返回值**：

- 返回中断源的状态（SET或RESET）

**作用**：检查指定的I2C中断源是否被使能

### __HAL_I2C_GET_FLAG

**宏原型**：

```c
#define __HAL_I2C_GET_FLAG(__HANDLE__, __FLAG__) ((((uint8_t)((__FLAG__) >> 16U)) == 0x01U) ? \
                                                  (((((__HANDLE__)->Instance->SR1) & ((__FLAG__) & I2C_FLAG_MASK)) == ((__FLAG__) & I2C_FLAG_MASK)) ? SET : RESET) : \
                                                  (((((__HANDLE__)->Instance->SR2) & ((__FLAG__) & I2C_FLAG_MASK)) == ((__FLAG__) & I2C_FLAG_MASK)) ? SET : RESET))
```

**参数**：

- `__HANDLE__`：I2C句柄
- `__FLAG__`：要检查的标志，可以是以下值之一：
  - I2C_FLAG_OVR: 超限/欠限标志
  - I2C_FLAG_AF: 应答失败标志
  - I2C_FLAG_ARLO: 仲裁丢失标志
  - I2C_FLAG_BERR: 总线错误标志
  - I2C_FLAG_TXE: 数据寄存器空标志
  - I2C_FLAG_RXNE: 数据寄存器非空标志
  - I2C_FLAG_STOPF: 停止检测标志
  - I2C_FLAG_ADD10: 10位头发送标志
  - I2C_FLAG_BTF: 字节传输完成标志
  - I2C_FLAG_ADDR: 地址发送标志/地址匹配标志
  - I2C_FLAG_SB: 起始位标志
  - I2C_FLAG_DUALF: 双地址标志
  - I2C_FLAG_GENCALL: 通用调用头标志
  - I2C_FLAG_TRA: 发送/接收标志
  - I2C_FLAG_BUSY: 总线忙标志
  - I2C_FLAG_MSL: 主机/从机标志

**返回值**：

- 返回标志的状态（SET或RESET）

**作用**：检查指定的I2C标志是否被置位

### __HAL_I2C_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_I2C_CLEAR_FLAG(__HANDLE__, __FLAG__) ((__HANDLE__)->Instance->SR1 = ~((__FLAG__) & I2C_FLAG_MASK))
```

**参数**：

- `__HANDLE__`：I2C句柄
- `__FLAG__`：要清除的标志，可以是以下值的任意组合：
  - I2C_FLAG_OVR: 超限/欠限标志（从机模式）
  - I2C_FLAG_AF: 应答失败标志
  - I2C_FLAG_ARLO: 仲裁丢失标志（主机模式）
  - I2C_FLAG_BERR: 总线错误标志

**返回值**：无

**作用**：清除I2C挂起的标志

### __HAL_I2C_CLEAR_ADDRFLAG

**宏原型**：

```c
#define __HAL_I2C_CLEAR_ADDRFLAG(__HANDLE__)    \
  do{                                           \
    __IO uint32_t tmpreg = 0x00U;               \
    tmpreg = (__HANDLE__)->Instance->SR1;       \
    tmpreg = (__HANDLE__)->Instance->SR2;       \
    UNUSED(tmpreg);                             \
  } while(0)
```

**参数**：

- `__HANDLE__`：I2C句柄

**返回值**：无

**作用**：清除I2C ADDR挂起标志

### __HAL_I2C_CLEAR_STOPFLAG

**宏原型**：

```c
#define __HAL_I2C_CLEAR_STOPFLAG(__HANDLE__)           \
  do{                                                  \
    __IO uint32_t tmpreg = 0x00U;                      \
    tmpreg = (__HANDLE__)->Instance->SR1;              \
    SET_BIT((__HANDLE__)->Instance->CR1, I2C_CR1_PE);  \
    UNUSED(tmpreg);                                    \
  } while(0)
```

**参数**：

- `__HANDLE__`：I2C句柄

**返回值**：无

**作用**：清除I2C STOPF挂起标志

### __HAL_I2C_ENABLE

**宏原型**：

```c
#define __HAL_I2C_ENABLE(__HANDLE__)                  SET_BIT((__HANDLE__)->Instance->CR1, I2C_CR1_PE)
```

**参数**：

- `__HANDLE__`：I2C句柄

**返回值**：无

**作用**：使能指定的I2C外设

### __HAL_I2C_DISABLE

**宏原型**：

```c
#define __HAL_I2C_DISABLE(__HANDLE__)                 CLEAR_BIT((__HANDLE__)->Instance->CR1, I2C_CR1_PE)
```

**参数**：

- `__HANDLE__`：I2C句柄

**返回值**：无

**作用**：禁用指定的I2C外设

## 2. I2C 初始化函数

### HAL_I2C_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Init(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或操作失败

**作用**：根据I2C_InitTypeDef中指定的参数初始化I2C模块

### HAL_I2C_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_DeInit(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或操作失败

**作用**：将I2C外设重置为默认状态

### HAL_I2C_MspInit

**函数原型**：

```c
void HAL_I2C_MspInit(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：初始化I2C底层资源（弱定义函数，需要用户实现）

### HAL_I2C_MspDeInit

**函数原型**：

```c
void HAL_I2C_MspDeInit(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：反初始化I2C底层资源（弱定义函数，需要用户实现）

## 3. I2C Master模式收发函数

### HAL_I2C_Master_Transmit

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Master_Transmit(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `Timeout`：超时时间

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中
- `HAL_TIMEOUT`：发送超时

**作用**：在主机模式下以阻塞模式发送指定数量的数据

### HAL_I2C_Master_Receive

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Master_Receive(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `Timeout`：超时时间

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中
- `HAL_TIMEOUT`：接收超时

**作用**：在主机模式下以阻塞模式接收指定数量的数据

### HAL_I2C_Master_Transmit_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Transmit_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在主机模式下以中断模式发送指定数量的数据

### HAL_I2C_Master_Receive_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Receive_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在主机模式下以中断模式接收指定数量的数据

### HAL_I2C_Master_Transmit_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Transmit_DMA(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在主机模式下以DMA模式发送指定数量的数据

### HAL_I2C_Master_Receive_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Receive_DMA(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在主机模式下以DMA模式接收指定数量的数据

## 4. I2C Slave模式收发函数

### HAL_I2C_Slave_Transmit

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Slave_Transmit(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中
- `HAL_TIMEOUT`：发送超时

**作用**：在从机模式下以阻塞模式发送指定数量的数据

### HAL_I2C_Slave_Receive

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Slave_Receive(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中
- `HAL_TIMEOUT`：接收超时

**作用**：在从机模式下以阻塞模式接收指定数量的数据

### HAL_I2C_Slave_Transmit_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Slave_Transmit_IT(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在从机模式下以中断模式发送指定数量的数据

### HAL_I2C_Slave_Receive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Slave_Receive_IT(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在从机模式下以中断模式接收指定数量的数据

### HAL_I2C_Slave_Transmit_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Slave_Transmit_DMA(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在从机模式下以DMA模式发送指定数量的数据

### HAL_I2C_Slave_Receive_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Slave_Receive_DMA(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在从机模式下以DMA模式接收指定数量的数据

## 5. I2C Mem模式收发函数

### HAL_I2C_Mem_Write

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Mem_Write(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint16_t MemAddress, uint16_t MemAddSize, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `MemAddress`：内部存储器地址
- `MemAddSize`：内部存储器地址大小
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中
- `HAL_TIMEOUT`：发送超时

**作用**：以阻塞模式向指定存储器地址写入数据

### HAL_I2C_Mem_Read

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Mem_Read(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint16_t MemAddress, uint16_t MemAddSize, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `MemAddress`：内部存储器地址
- `MemAddSize`：内部存储器地址大小
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中
- `HAL_TIMEOUT`：接收超时

**作用**：以阻塞模式从指定存储器地址读取数据

### HAL_I2C_Mem_Write_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Mem_Write_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint16_t MemAddress, uint16_t MemAddSize, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `MemAddress`：内部存储器地址
- `MemAddSize`：内部存储器地址大小
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：以中断模式向指定存储器地址写入数据

### HAL_I2C_Mem_Read_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Mem_Read_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint16_t MemAddress, uint16_t MemAddSize, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `MemAddress`：内部存储器地址
- `MemAddSize`：内部存储器地址大小
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：以中断模式从指定存储器地址读取数据

### HAL_I2C_Mem_Write_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Mem_Write_DMA(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint16_t MemAddress, uint16_t MemAddSize, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `MemAddress`：内部存储器地址
- `MemAddSize`：内部存储器地址大小
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：以DMA模式向指定存储器地址写入数据

### HAL_I2C_Mem_Read_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Mem_Read_DMA(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint16_t MemAddress, uint16_t MemAddSize, uint8_t *pData, uint16_t Size);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `MemAddress`：内部存储器地址
- `MemAddSize`：内部存储器地址大小
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：以DMA模式从指定存储器地址读取数据

## 6. I2C Seq模式收发函数

### HAL_I2C_Master_Seq_Transmit_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Master_Seq_Transmit_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在主机模式下以中断模式序列发送指定数量的数据

### HAL_I2C_Master_Seq_Receive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Master_Seq_Receive_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在主机模式下以中断模式序列接收指定数量的数据

### HAL_I2C_Slave_Seq_Transmit_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Slave_Seq_Transmit_IT(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在从机模式下以中断模式序列发送指定数量的数据

### HAL_I2C_Slave_Seq_Receive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_I2C_Slave_Seq_Receive_IT(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在从机模式下以中断模式序列接收指定数量的数据

### HAL_I2C_Master_Seq_Transmit_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Seq_Transmit_DMA(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在主机模式下以DMA模式序列发送指定数量的数据

### HAL_I2C_Master_Seq_Receive_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Seq_Receive_DMA(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在主机模式下以DMA模式序列接收指定数量的数据

### HAL_I2C_Slave_Seq_Transmit_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Slave_Seq_Transmit_DMA(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要发送的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：发送操作正在进行中

**作用**：在从机模式下以DMA模式序列发送指定数量的数据

### HAL_I2C_Slave_Seq_Receive_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Slave_Seq_Receive_DMA(I2C_HandleTypeDef *hi2c, uint8_t *pData, uint16_t Size, uint32_t XferOptions);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pData`：指向数据缓冲区的指针
- `Size`：要接收的数据量
- `XferOptions`：传输选项，可以是以下值之一：
  - I2C_FIRST_FRAME: 无序列使用，功能与非序列模式中的相关接口相同
  - I2C_FIRST_AND_NEXT_FRAME: 序列使用，此选项允许管理带有起始条件、地址和数据的序列传输，但没有最终停止条件
  - I2C_NEXT_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且没有最终停止条件
  - I2C_FIRST_AND_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME: 序列使用，此选项允许管理带有重新开始条件、地址和新数据的序列传输（如果方向改变）或仅管理新数据（如果没有方向改变）且带有最终停止条件
  - I2C_LAST_FRAME_NO_STOP: 序列使用（仅主机），此选项允许在多次调用相同的主机序列接口后管理重新开始条件

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：接收操作正在进行中

**作用**：在从机模式下以DMA模式序列接收指定数量的数据

## 7. I2C 状态获取函数

### HAL_I2C_GetState

**函数原型**：

```c
HAL_I2C_StateTypeDef HAL_I2C_GetState(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- 返回I2C句柄状态

**作用**：返回I2C句柄状态

### HAL_I2C_GetMode

**函数原型**：

```c
HAL_I2C_ModeTypeDef HAL_I2C_GetMode(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- 返回I2C模式

**作用**：返回I2C模式（主机、从机、内存或无模式）

### HAL_I2C_GetError

**函数原型**：

```c
uint32_t HAL_I2C_GetError(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- 返回I2C错误码

**作用**：返回I2C错误码

## 8. I2C中断处理函数

### HAL_I2C_EV_IRQHandler

**函数原型**：
```c
void HAL_I2C_EV_IRQHandler(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理I2C事件中断请求

### HAL_I2C_ER_IRQHandler

**函数原型**：
```c
void HAL_I2C_ER_IRQHandler(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理I2C错误中断请求

## 9. I2C 回调函数

### HAL_I2C_MasterTxCpltCallback

**函数原型**：
```c
void HAL_I2C_MasterTxCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：主机发送传输完成回调函数

### HAL_I2C_MasterRxCpltCallback

**函数原型**：
```c
void HAL_I2C_MasterRxCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：主机接收传输完成回调函数

### HAL_I2C_SlaveTxCpltCallback

**函数原型**：
```c
void HAL_I2C_SlaveTxCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：从机发送传输完成回调函数

### HAL_I2C_SlaveRxCpltCallback

**函数原型**：
```c
void HAL_I2C_SlaveRxCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：从机接收传输完成回调函数

### HAL_I2C_MemTxCpltCallback

**函数原型**：

```c
void HAL_I2C_MemTxCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：内存发送传输完成回调函数

### HAL_I2C_MemRxCpltCallback

**函数原型**：

```c
void HAL_I2C_MemRxCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：内存接收传输完成回调函数

### HAL_I2C_AbortCpltCallback

**函数原型**：

```c
void HAL_I2C_AbortCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：I2C中止传输完成回调函数

### HAL_I2C_AddrCallback

**函数原型**：
```c
void HAL_I2C_AddrCallback(I2C_HandleTypeDef *hi2c, uint8_t TransferDirection, uint16_t AddrMatchCode);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `TransferDirection`：主机请求的传输方向（写/读），可以是以下值之一：
  - I2C_DIRECTION_RECEIVE: 接收方向
  - I2C_DIRECTION_TRANSMIT: 发送方向
- `AddrMatchCode`：地址匹配码

**返回值**：无

**作用**：从机地址匹配回调函数

### HAL_I2C_ListenCpltCallback

**函数原型**：
```c
void HAL_I2C_ListenCpltCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：监听完成回调函数



### HAL_I2C_ErrorCallback

**函数原型**：
```c
void HAL_I2C_ErrorCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：
- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：无

**作用**：I2C错误回调函数

### HAL_I2C_RegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_RegisterCallback(I2C_HandleTypeDef *hi2c, HAL_I2C_CallbackIDTypeDef CallbackID, pI2C_CallbackTypeDef pCallback);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `CallbackID`：回调ID，可以是以下值之一：
  - HAL_I2C_MASTER_TX_COMPLETE_CB_ID: 主机发送传输完成回调ID
  - HAL_I2C_MASTER_RX_COMPLETE_CB_ID: 主机接收传输完成回调ID
  - HAL_I2C_SLAVE_TX_COMPLETE_CB_ID: 从机发送传输完成回调ID
  - HAL_I2C_SLAVE_RX_COMPLETE_CB_ID: 从机接收传输完成回调ID
  - HAL_I2C_LISTEN_COMPLETE_CB_ID: 监听完成回调ID
  - HAL_I2C_MEM_TX_COMPLETE_CB_ID: 内存发送传输完成回调ID
  - HAL_I2C_MEM_RX_COMPLETE_CB_ID: 内存接收传输完成回调ID
  - HAL_I2C_ERROR_CB_ID: 错误回调ID
  - HAL_I2C_ABORT_CB_ID: 中止回调ID
  - HAL_I2C_MSPINIT_CB_ID: MspInit回调ID
  - HAL_I2C_MSPDEINIT_CB_ID: MspDeInit回调ID
- `pCallback`：指向回调函数的指针

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或操作失败

**作用**：注册用户I2C回调函数，用于替代弱预定义回调

### HAL_I2C_UnRegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_UnRegisterCallback(I2C_HandleTypeDef *hi2c, HAL_I2C_CallbackIDTypeDef CallbackID);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `CallbackID`：回调ID，可以是以下值之一：
  - HAL_I2C_MASTER_TX_COMPLETE_CB_ID: 主机发送传输完成回调ID
  - HAL_I2C_MASTER_RX_COMPLETE_CB_ID: 主机接收传输完成回调ID
  - HAL_I2C_SLAVE_TX_COMPLETE_CB_ID: 从机发送传输完成回调ID
  - HAL_I2C_SLAVE_RX_COMPLETE_CB_ID: 从机接收传输完成回调ID
  - HAL_I2C_LISTEN_COMPLETE_CB_ID: 监听完成回调ID
  - HAL_I2C_MEM_TX_COMPLETE_CB_ID: 内存发送传输完成回调ID
  - HAL_I2C_MEM_RX_COMPLETE_CB_ID: 内存接收传输完成回调ID
  - HAL_I2C_ERROR_CB_ID: 错误回调ID
  - HAL_I2C_ABORT_CB_ID: 中止回调ID
  - HAL_I2C_MSPINIT_CB_ID: MspInit回调ID
  - HAL_I2C_MSPDEINIT_CB_ID: MspDeInit回调ID

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或操作失败

**作用**：注销I2C回调函数，将其重定向到弱预定义回调

### HAL_I2C_RegisterAddrCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_RegisterAddrCallback(I2C_HandleTypeDef *hi2c, pI2C_AddrCallbackTypeDef pCallback);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `pCallback`：指向地址匹配回调函数的指针

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或操作失败

**作用**：注册从机地址匹配I2C回调函数，用于替代弱HAL_I2C_AddrCallback()预定义回调

### HAL_I2C_UnRegisterAddrCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_UnRegisterAddrCallback(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或操作失败

**作用**：注销从机地址匹配I2C回调函数，将其重定向到弱HAL_I2C_AddrCallback()预定义回调

## 10. I2C 其它控制函数

### HAL_I2C_IsDeviceReady

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_IsDeviceReady(I2C_HandleTypeDef *hi2c, uint16_t DevAddress, uint32_t Trials, uint32_t Timeout);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位
- `Trials`：尝试次数
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：设备准备好
- `HAL_ERROR`：参数错误或设备未准备好
- `HAL_BUSY`：设备忙
- `HAL_TIMEOUT`：超时

**作用**：检查目标设备是否准备好进行通信

### HAL_I2C_EnableListen_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_EnableListen_IT(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：设备忙

**作用**：使用中断使能地址监听模式

### HAL_I2C_DisableListen_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_DisableListen_IT(I2C_HandleTypeDef *hi2c);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误
- `HAL_BUSY`：设备忙

**作用**：使用中断禁用地址监听模式

### HAL_I2C_Master_Abort_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_I2C_Master_Abort_IT(I2C_HandleTypeDef *hi2c, uint16_t DevAddress);
```

**参数**：

- `hi2c`：指向包含I2C配置信息的I2C_HandleTypeDef结构体的指针
- `DevAddress`：目标设备地址：数据手册中的7位地址值在调用接口前必须左移一位

**返回值**：

- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误或错误使用中止函数

**作用**：使用中断中止主机I2C过程通信
