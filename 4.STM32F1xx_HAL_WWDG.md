# STM32F1xx HAL WWDG库函数说明文档

本文档介绍了STM32F1xx HAL库中的WWDG（窗口看门狗）函数，包括函数原型、参数、返回值和具体作用。

## 1. WWDG __HAL 宏函数

### __HAL_WWDG_ENABLE

**宏原型**：

```c
#define __HAL_WWDG_ENABLE(__HANDLE__)                         SET_BIT((__HANDLE__)->Instance->CR, WWDG_CR_WDGA)
```

**参数**：

- `__HANDLE__`：WWDG句柄

**返回值**：无

**作用**：启用WWDG外设

### __HAL_WWDG_ENABLE_IT

**宏原型**：

```c
#define __HAL_WWDG_ENABLE_IT(__HANDLE__, __INTERRUPT__)       SET_BIT((__HANDLE__)->Instance->CFR, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：WWDG句柄
- `__INTERRUPT__`：指定要启用的中断，可以是WWDG_IT_EWI（提前唤醒中断）

**返回值**：无

**作用**：启用WWDG提前唤醒中断。注意：一旦启用，此中断只能通过系统复位禁用。

### __HAL_WWDG_GET_IT

**宏原型**：

```c
#define __HAL_WWDG_GET_IT(__HANDLE__, __INTERRUPT__)        __HAL_WWDG_GET_FLAG((__HANDLE__),(__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：WWDG句柄
- `__INTERRUPT__`：指定要检查的中断，可以是WWDG_FLAG_EWIF（提前唤醒中断标志）

**返回值**：

- WWDG_FLAG的新状态（SET或RESET）

**作用**：检查选定的WWDG中断是否已发生

### __HAL_WWDG_CLEAR_IT

**宏原型**：

```c
#define __HAL_WWDG_CLEAR_IT(__HANDLE__, __INTERRUPT__)      __HAL_WWDG_CLEAR_FLAG((__HANDLE__), (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：WWDG句柄
- `__INTERRUPT__`：指定要清除的中断挂起位，可以是WWDG_FLAG_EWIF（提前唤醒中断标志）

**返回值**：无

**作用**：清除WWDG中断挂起位

### __HAL_WWDG_GET_FLAG

**宏原型**：

```c
#define __HAL_WWDG_GET_FLAG(__HANDLE__, __FLAG__)           (((__HANDLE__)->Instance->SR & (__FLAG__)) == (__FLAG__))
```

**参数**：

- `__HANDLE__`：WWDG句柄
- `__FLAG__`：指定要检查的标志，可以是WWDG_FLAG_EWIF（提前唤醒中断标志）

**返回值**：

- WWDG_FLAG的新状态（SET或RESET）

**作用**：检查指定的WWDG标志是否置位

### __HAL_WWDG_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_WWDG_CLEAR_FLAG(__HANDLE__, __FLAG__)         ((__HANDLE__)->Instance->SR = ~(__FLAG__))
```

**参数**：

- `__HANDLE__`：WWDG句柄
- `__FLAG__`：指定要清除的标志，可以是WWDG_FLAG_EWIF（提前唤醒中断标志）

**返回值**：无

**作用**：清除WWDG的挂起标志

### __HAL_WWDG_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_WWDG_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) (((__HANDLE__)->Instance->CFR & (__INTERRUPT__)) == (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：WWDG句柄
- `__INTERRUPT__`：指定要检查的WWDG中断源，可以是WWDG_IT_EWI（提前唤醒中断）

**返回值**：

- __INTERRUPT__的状态（TRUE或FALSE）

**作用**：检查指定的WWDG中断源是否启用

## 2. WWDG初始化函数

### HAL_WWDG_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_WWDG_Init(WWDG_HandleTypeDef *hwwdg);
```

**参数**：
- `hwwdg`：指向包含WWDG配置信息的WWDG_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：参数错误

**作用**：根据WWDG_HandleTypeDef中指定的参数初始化WWDG

### HAL_WWDG_MspInit

**函数原型**：
```c
void HAL_WWDG_MspInit(WWDG_HandleTypeDef *hwwdg);
```

**参数**：
- `hwwdg`：指向包含WWDG配置信息的WWDG_HandleTypeDef结构体的指针

**返回值**：无

**作用**：初始化WWDG的MSP（MCU特定包）。这是一个弱函数，用户可以在其应用程序文件中重新实现。

## 3. WWDG操作函数

### HAL_WWDG_Refresh

**函数原型**：

```c
HAL_StatusTypeDef HAL_WWDG_Refresh(WWDG_HandleTypeDef *hwwdg);
```

**参数**：

- `hwwdg`：指向包含WWDG配置信息的WWDG_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：操作成功

**作用**：刷新WWDG，将计数器值重新加载到计数器中，防止WWDG复位，简称喂狗



## 4. WWDG中断函数

### HAL_WWDG_IRQHandler

**函数原型**：

```c
void HAL_WWDG_IRQHandler(WWDG_HandleTypeDef *hwwdg);
```

**参数**：

- `hwwdg`：指向包含WWDG配置信息的WWDG_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理WWDG中断请求。提前唤醒中断(EWI)可用于在实际复位产生前执行特定的安全操作或数据记录。

## 5. WWDG回调函数

### HAL_WWDG_EarlyWakeupCallback

**函数原型**：

```c
void HAL_WWDG_EarlyWakeupCallback(WWDG_HandleTypeDef *hwwdg);
```

**参数**：

- `hwwdg`：指向包含WWDG配置信息的WWDG_HandleTypeDef结构体的指针

**返回值**：无

**作用**：WWDG提前唤醒回调函数。这是一个弱函数，用户可以在其应用程序文件中重新实现。

### HAL_WWDG_RegisterCallback

**函数原型**：
```c
HAL_StatusTypeDef HAL_WWDG_RegisterCallback(WWDG_HandleTypeDef *hwwdg, HAL_WWDG_CallbackIDTypeDef CallbackID, pWWDG_CallbackTypeDef pCallback);
```

**参数**：
- `hwwdg`：WWDG句柄
- `CallbackID`：要注册的回调ID，可以是以下值：
  - `HAL_WWDG_EWI_CB_ID`：提前唤醒中断回调ID
  - `HAL_WWDG_MSPINIT_CB_ID`：MspInit回调ID
- `pCallback`：指向回调函数的指针

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：操作失败

**作用**：注册用户WWDG回调函数

### HAL_WWDG_UnRegisterCallback

**函数原型**：
```c
HAL_StatusTypeDef HAL_WWDG_UnRegisterCallback(WWDG_HandleTypeDef *hwwdg, HAL_WWDG_CallbackIDTypeDef CallbackID);
```

**参数**：
- `hwwdg`：WWDG句柄
- `CallbackID`：要注销的回调ID，可以是以下值：
  - `HAL_WWDG_EWI_CB_ID`：提前唤醒中断回调ID
  - `HAL_WWDG_MSPINIT_CB_ID`：MspInit回调ID

**返回值**：
- `HAL_OK`：操作成功
- `HAL_ERROR`：操作失败

**作用**：注销WWDG回调函数，回调被重定向到弱（覆盖）的预定义回调

 