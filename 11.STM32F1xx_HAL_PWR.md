# STM32F1xx HAL PWR库函数说明文档

• Sleep：几乎透明，注意清中断即可。
• Stop：醒来第一件事“**重配时钟** + 重新开外设”，否则stm32会默认使用内部低速时钟(HSI)。
• Standby：醒来就是“重启”，所有 SRAM/寄存器值已丢失，只能靠备份域或 Flash 恢复现场。

|  模式   |               断电区域                |                    如何进入                     |                      如何唤醒（典型源）                      |
| :-----: | :-----------------------------------: | :---------------------------------------------: | :----------------------------------------------------------: |
|  Sleep  | CPU**时钟停**<br>外设供电<br>SRAM供电 |              `__WFI()` / `__WFE()`              |        任意 NVIC 中断或事件（GPIO、USART、SysTick…）         |
|  Stop   |  CPU时钟停<br/>外设断电<br/>SRAM供电  |  `PWR_CR1` 设 Stop 位 → `__WFI()` / `__WFE()`   | 任一 EXTI 线（GPIO WKUP 引脚）、RTC 闹钟、LPTIM、USB FS 唤醒 |
| Standby |  CPU时钟停<br/>外设断电<br/>SRAM断电  | `PWR_CR1` 设 Standby 位 → `__WFI()` / `__WFE()` |    WKUP引脚上升沿、RTC 闹钟/时间戳、独立看门狗复位、NRST     |

## 1. PWR __HAL 宏函数

### __HAL_PWR_GET_FLAG

**宏原型**：

```c
#define __HAL_PWR_GET_FLAG(__FLAG__) ((PWR->CSR & (__FLAG__)) == (__FLAG__))
```

**参数**：

- `__FLAG__`：要检查的标志，可以是以下值：
  - `PWR_FLAG_WU`：唤醒标志
  - `PWR_FLAG_SB`：待机标志
  - `PWR_FLAG_PVDO`：PVD输出标志

**返回值**：

- 返回标志状态（TRUE或FALSE）

**作用**：检查指定的PWR标志是否被置位

### __HAL_PWR_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_PWR_CLEAR_FLAG(__FLAG__) SET_BIT(PWR->CR, ((__FLAG__) << 2))
```

**参数**：

- `__FLAG__`：要清除的标志，可以是以下值：
  - `PWR_FLAG_WU`：唤醒标志
  - `PWR_FLAG_SB`：待机标志

**返回值**：无

**作用**：清除PWR的挂起标志

### __HAL_PWR_PVD_EXTI_ENABLE_IT

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_ENABLE_IT() SET_BIT(EXTI->IMR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：使能PVD EXTI线16上的中断

### __HAL_PWR_PVD_EXTI_DISABLE_IT

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_DISABLE_IT() CLEAR_BIT(EXTI->IMR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：禁用PVD EXTI线16上的中断

### __HAL_PWR_PVD_EXTI_ENABLE_EVENT

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_ENABLE_EVENT() SET_BIT(EXTI->EMR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：使能PVD EXTI线16上的事件

### __HAL_PWR_PVD_EXTI_DISABLE_EVENT

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_DISABLE_EVENT() CLEAR_BIT(EXTI->EMR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：禁用PVD EXTI线16上的事件

### __HAL_PWR_PVD_EXTI_ENABLE_FALLING_EDGE

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_ENABLE_FALLING_EDGE() SET_BIT(EXTI->FTSR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：配置PVD EXTI线为下降沿触发

### __HAL_PWR_PVD_EXTI_DISABLE_FALLING_EDGE

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_DISABLE_FALLING_EDGE() CLEAR_BIT(EXTI->FTSR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：禁用PVD EXTI下降沿触发

### __HAL_PWR_PVD_EXTI_ENABLE_RISING_EDGE

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_ENABLE_RISING_EDGE() SET_BIT(EXTI->RTSR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：配置PVD EXTI线为上升沿触发

### __HAL_PWR_PVD_EXTI_DISABLE_RISING_EDGE

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_DISABLE_RISING_EDGE() CLEAR_BIT(EXTI->RTSR, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：禁用PVD EXTI上升沿触发

### __HAL_PWR_PVD_EXTI_ENABLE_RISING_FALLING_EDGE

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_ENABLE_RISING_FALLING_EDGE() __HAL_PWR_PVD_EXTI_ENABLE_RISING_EDGE();__HAL_PWR_PVD_EXTI_ENABLE_FALLING_EDGE();
```

**参数**：无

**返回值**：无

**作用**：配置PVD EXTI线为上升沿和下降沿触发

### __HAL_PWR_PVD_EXTI_DISABLE_RISING_FALLING_EDGE

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_DISABLE_RISING_FALLING_EDGE() __HAL_PWR_PVD_EXTI_DISABLE_RISING_EDGE();__HAL_PWR_PVD_EXTI_DISABLE_FALLING_EDGE();
```

**参数**：无

**返回值**：无

**作用**：禁用PVD EXTI上升沿和下降沿触发

### __HAL_PWR_PVD_EXTI_GET_FLAG

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_GET_FLAG() (EXTI->PR & (PWR_EXTI_LINE_PVD))
```

**参数**：无

**返回值**：

- 返回PVD EXTI线的状态

**作用**：检查指定的PVD EXTI中断标志是否置位

### __HAL_PWR_PVD_EXTI_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_CLEAR_FLAG() (EXTI->PR = (PWR_EXTI_LINE_PVD))
```

**参数**：无

**返回值**：无

**作用**：清除PVD EXTI标志

### __HAL_PWR_PVD_EXTI_GENERATE_SWIT

**宏原型**：

```c
#define __HAL_PWR_PVD_EXTI_GENERATE_SWIT() SET_BIT(EXTI->SWIER, PWR_EXTI_LINE_PVD)
```

**参数**：无

**返回值**：无

**作用**：在选定的EXTI线上生成软件中断

## 2. PWR初始化函数

### HAL_PWR_DeInit

**函数原型**：

```c
void HAL_PWR_DeInit(void);
```

**参数**：无

**返回值**：无

**作用**：将PWR外设寄存器去初始化到默认复位值

### HAL_PWR_EnableBkUpAccess

**函数原型**：

```c
void HAL_PWR_EnableBkUpAccess(void);
```

**参数**：无

**返回值**：无

**作用**：使能对备份域（RTC寄存器、RTC备份数据寄存器）的访问

### HAL_PWR_DisableBkUpAccess

**函数原型**：

```c
void HAL_PWR_DisableBkUpAccess(void);
```

**参数**：无

**返回值**：无

**作用**：禁止对备份域（RTC寄存器、RTC备份数据寄存器）的访问

## 3. PVD配置函数

### HAL_PWR_ConfigPVD

**函数原型**：

```c
void HAL_PWR_ConfigPVD(PWR_PVDTypeDef *sConfigPVD);
```

**参数**：

- `sConfigPVD`：指向PWR_PVDTypeDef结构体的指针，包含PVD配置信息
  - `PVDLevel`：PVD检测电平，可以是以下值：
    - `PWR_PVDLEVEL_0`：2.2V
    - `PWR_PVDLEVEL_1`：2.3V
    - `PWR_PVDLEVEL_2`：2.4V
    - `PWR_PVDLEVEL_3`：2.5V
    - `PWR_PVDLEVEL_4`：2.6V
    - `PWR_PVDLEVEL_5`：2.7V
    - `PWR_PVDLEVEL_6`：2.8V
    - `PWR_PVDLEVEL_7`：2.9V
  - `Mode`：工作模式，可以是以下值：
    - `PWR_PVD_MODE_NORMAL`：普通模式
    - `PWR_PVD_MODE_IT_RISING`：中断模式，上升沿触发
    - `PWR_PVD_MODE_IT_FALLING`：中断模式，下降沿触发
    - `PWR_PVD_MODE_IT_RISING_FALLING`：中断模式，上升/下降沿触发
    - `PWR_PVD_MODE_EVENT_RISING`：事件模式，上升沿触发
    - `PWR_PVD_MODE_EVENT_FALLING`：事件模式，下降沿触发
    - `PWR_PVD_MODE_EVENT_RISING_FALLING`：事件模式，上升/下降沿触发

**返回值**：无

**作用**：根据sConfigPVD中指定的参数配置PVD

### HAL_PWR_EnablePVD

**函数原型**：

```c
void HAL_PWR_EnablePVD(void);
```

**参数**：无

**返回值**：无

**作用**：使能电源电压检测器(PVD)

### HAL_PWR_DisablePVD

**函数原型**：

```c
void HAL_PWR_DisablePVD(void);
```

**参数**：无

**返回值**：无

**作用**：禁用电源电压检测器(PVD)

## 4. 唤醒引脚配置函数

### HAL_PWR_EnableWakeUpPin

**函数原型**：

```c
void HAL_PWR_EnableWakeUpPin(uint32_t WakeUpPinx);
```

**参数**：

- `WakeUpPinx`：指定要唤醒的引脚，可以是以下值：
  - `PWR_WAKEUP_PIN1`：唤醒引脚1（PA0）

**返回值**：无

**作用**：使能WakeUp PINx功能

### HAL_PWR_DisableWakeUpPin

**函数原型**：

```c
void HAL_PWR_DisableWakeUpPin(uint32_t WakeUpPinx);
```

**参数**：

- `WakeUpPinx`：指定要禁用的唤醒引脚，可以是以下值：
  - `PWR_WAKEUP_PIN1`：唤醒引脚1（PA0）

**返回值**：无

**作用**：禁用WakeUp PINx功能

## 5. 低功耗模式配置函数

### HAL_PWR_EnterSLEEPMode

**函数原型**：

```c
void HAL_PWR_EnterSLEEPMode(uint32_t Regulator, uint8_t SLEEPEntry);
```

**参数**：

- `Regulator`：调节器状态（在SLEEP模式下无影响，仅用于兼容性）
- `SLEEPEntry`：指定SLEEP模式的进入方式，可以是以下值：
  - `PWR_SLEEPENTRY_WFI`：使用WFI指令进入SLEEP模式
  - `PWR_SLEEPENTRY_WFE`：使用WFE指令进入SLEEP模式

**返回值**：无

**作用**：进入SLEEP模式

### HAL_PWR_EnterSTOPMode

**函数原型**：

```c
void HAL_PWR_EnterSTOPMode(uint32_t Regulator, uint8_t STOPEntry);
```

**参数**：

- `Regulator`：指定STOP模式下的调节器状态，可以是以下值：
  - `PWR_MAINREGULATOR_ON`：STOP模式下主调节器开启
  - `PWR_LOWPOWERREGULATOR_ON`：STOP模式下低功耗调节器开启
- `STOPEntry`：指定STOP模式的进入方式，可以是以下值：
  - `PWR_STOPENTRY_WFI`：使用WFI指令进入STOP模式
  - `PWR_STOPENTRY_WFE`：使用WFE指令进入STOP模式

**返回值**：无

**作用**：进入STOP模式

### HAL_PWR_EnterSTANDBYMode

**函数原型**：

```c
void HAL_PWR_EnterSTANDBYMode(void);
```

**参数**：无

**返回值**：无

**作用**：进入STANDBY模式

## 6. 低功耗模式辅助函数

### HAL_PWR_EnableSleepOnExit

**函数原型**：

```c
void HAL_PWR_EnableSleepOnExit(void);
```

**参数**：无

**返回值**：无

**作用**：使能在从处理程序模式返回到线程模式时的Sleep-On-Exit功能

### HAL_PWR_DisableSleepOnExit

**函数原型**：

```c
void HAL_PWR_DisableSleepOnExit(void);
```

**参数**：无

**返回值**：无

**作用**：禁用Sleep-On-Exit功能

### HAL_PWR_EnableSEVOnPend

**函数原型**：

```c
void HAL_PWR_EnableSEVOnPend(void);
```

**参数**：无

**返回值**：无

**作用**：使能CORTEX M3 SEVONPEND位

### HAL_PWR_DisableSEVOnPend

**函数原型**：

```c
void HAL_PWR_DisableSEVOnPend(void);
```

**参数**：无

**返回值**：无

**作用**：禁用CORTEX M3 SEVONPEND位

## 7. PVD中断处理函数

### HAL_PWR_PVD_IRQHandler

**函数原型**：

```c
void HAL_PWR_PVD_IRQHandler(void);
```

**参数**：无

**返回值**：无

**作用**：处理PWR PVD中断请求

## 8. PVD回调函数

### HAL_PWR_PVDCallback

**函数原型**：

```c
void HAL_PWR_PVDCallback(void);
```

**参数**：无

**返回值**：无

**作用**：PWR PVD中断回调函数，在HAL_PWR_PVD_IRQHandler中被调用