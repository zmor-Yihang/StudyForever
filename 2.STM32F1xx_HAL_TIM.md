# STM32F1xx HAL 定时器库函数说明文档

本文档介绍了STM32F1xx HAL库中的定时器函数和宏，包括函数原型、参数、返回值和具体作用。

## 1. 定时器 __HAL 宏函数

### 1.1 定时器状态控制宏

#### __HAL_TIM_RESET_HANDLE_STATE

**宏原型**：

```c
#define __HAL_TIM_RESET_HANDLE_STATE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：重置TIM句柄的状态为HAL_TIM_STATE_RESET

#### __HAL_TIM_ENABLE

**宏原型**：

```c
#define __HAL_TIM_ENABLE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：使能定时器，通过设置TIM_CR1寄存器的CEN位

#### __HAL_TIM_MOE_ENABLE

**宏原型**：

```c
#define __HAL_TIM_MOE_ENABLE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：使能定时器的主输出（Main Output Enable），通过设置TIM_BDTR寄存器的MOE位

#### __HAL_TIM_DISABLE

**宏原型**：

```c
#define __HAL_TIM_DISABLE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：禁用定时器，通过清除TIM_CR1寄存器的CEN位

#### __HAL_TIM_MOE_DISABLE

**宏原型**：

```c
#define __HAL_TIM_MOE_DISABLE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：禁用定时器的主输出，通过清除TIM_BDTR寄存器的MOE位

#### __HAL_TIM_MOE_DISABLE_UNCONDITIONALLY

**宏原型**：

```c
#define __HAL_TIM_MOE_DISABLE_UNCONDITIONALLY(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：无条件禁用定时器的主输出，不考虑锁定级别

### 1.2 中断和DMA控制宏

#### __HAL_TIM_ENABLE_IT

**宏原型**：

```c
#define __HAL_TIM_ENABLE_IT(__HANDLE__, __INTERRUPT__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__INTERRUPT__`：要使能的中断

**返回值**：无

**作用**：使能定时器的指定中断，通过设置TIM_DIER寄存器的相应位

#### __HAL_TIM_DISABLE_IT

**宏原型**：

```c
#define __HAL_TIM_DISABLE_IT(__HANDLE__, __INTERRUPT__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__INTERRUPT__`：要禁用的中断

**返回值**：无

**作用**：禁用定时器的指定中断，通过清除TIM_DIER寄存器的相应位

#### __HAL_TIM_ENABLE_DMA

**宏原型**：

```c
#define __HAL_TIM_ENABLE_DMA(__HANDLE__, __DMA__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__DMA__`：要使能的DMA请求

**返回值**：无

**作用**：使能定时器的指定DMA请求，通过设置TIM_DIER寄存器的相应位

#### __HAL_TIM_DISABLE_DMA

**宏原型**：

```c
#define __HAL_TIM_DISABLE_DMA(__HANDLE__, __DMA__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__DMA__`：要禁用的DMA请求

**返回值**：无

**作用**：禁用定时器的指定DMA请求，通过清除TIM_DIER寄存器的相应位

### 1.3 标志和中断状态宏

#### __HAL_TIM_GET_FLAG

**宏原型**：

```c
#define __HAL_TIM_GET_FLAG(__HANDLE__, __FLAG__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__FLAG__`：要检查的标志

**返回值**：标志状态（SET或RESET）

**作用**：获取定时器的指定标志状态，通过读取TIM_SR寄存器的相应位

#### __HAL_TIM_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_TIM_CLEAR_FLAG(__HANDLE__, __FLAG__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__FLAG__`：要清除的标志

**返回值**：无

**作用**：清除定时器的指定标志，通过写0到TIM_SR寄存器的相应位

#### __HAL_TIM_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_TIM_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__INTERRUPT__`：要检查的中断源

**返回值**：中断源状态（使能或禁用）

**作用**：检查指定的定时器中断源是否使能，通过读取TIM_DIER寄存器的相应位

#### __HAL_TIM_CLEAR_IT

**宏原型**：

```c
#define __HAL_TIM_CLEAR_IT(__HANDLE__, __INTERRUPT__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__INTERRUPT__`：要清除的中断标志

**返回值**：无

**作用**：清除定时器的指定中断标志，通过写0到TIM_SR寄存器的相应位

### 1.4 计数器控制宏

#### __HAL_TIM_IS_TIM_COUNTING_DOWN

**宏原型**：

```c
#define __HAL_TIM_IS_TIM_COUNTING_DOWN(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：计数方向（向下计数为1，向上计数为0）

**作用**：检查定时器是否处于向下计数模式，通过读取TIM_CR1寄存器的DIR位

#### __HAL_TIM_SET_COUNTER

**宏原型**：

```c
#define __HAL_TIM_SET_COUNTER(__HANDLE__, __COUNTER__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__COUNTER__`：要设置的计数器值

**返回值**：无

**作用**：设置定时器的计数器值，通过写入TIM_CNT寄存器

#### __HAL_TIM_GET_COUNTER

**宏原型**：

```c
#define __HAL_TIM_GET_COUNTER(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：当前计数器值

**作用**：获取定时器的当前计数器值，通过读取TIM_CNT寄存器

#### __HAL_TIM_SET_AUTORELOAD

**宏原型**：

```c
#define __HAL_TIM_SET_AUTORELOAD(__HANDLE__, __AUTORELOAD__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__AUTORELOAD__`：要设置的自动重载值

**返回值**：无

**作用**：设置定时器的自动重载值，通过写入TIM_ARR寄存器

#### __HAL_TIM_GET_AUTORELOAD

**宏原型**：

```c
#define __HAL_TIM_GET_AUTORELOAD(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：当前自动重载值

**作用**：获取定时器的当前自动重载值，通过读取TIM_ARR寄存器

#### __HAL_TIM_SET_PRESCALER

**宏原型**：

```c
#define __HAL_TIM_SET_PRESCALER(__HANDLE__, __PRESC__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__PRESC__`：要设置的预分频值

**返回值**：无

**作用**：设置定时器的预分频值，通过写入TIM_PSC寄存器

### 1.5 时钟分频控制宏

#### __HAL_TIM_SET_CLOCKDIVISION

**宏原型**：

```c
#define __HAL_TIM_SET_CLOCKDIVISION(__HANDLE__, __CKD__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CKD__`：要设置的时钟分频值

**返回值**：无

**作用**：设置定时器的时钟分频值，通过写入TIM_CR1寄存器的CKD位

#### __HAL_TIM_GET_CLOCKDIVISION

**宏原型**：

```c
#define __HAL_TIM_GET_CLOCKDIVISION(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：当前时钟分频值

**作用**：获取定时器的当前时钟分频值，通过读取TIM_CR1寄存器的CKD位

### 1.6 输入捕获控制宏

#### __HAL_TIM_SET_ICPRESCALER

**宏原型**：

```c
#define __HAL_TIM_SET_ICPRESCALER(__HANDLE__, __CHANNEL__, __ICPSC__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）
- `__ICPSC__`：要设置的输入捕获预分频值

**返回值**：无

**作用**：设置定时器指定通道的输入捕获预分频值，通过写入TIM_CCMRx寄存器的ICxPSC位

#### __HAL_TIM_GET_ICPRESCALER

**宏原型**：

```c
#define __HAL_TIM_GET_ICPRESCALER(__HANDLE__, __CHANNEL__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：当前输入捕获预分频值

**作用**：获取定时器指定通道的输入捕获预分频值，通过读取TIM_CCMRx寄存器的ICxPSC位

### 1.7 比较寄存器控制宏

#### __HAL_TIM_SET_COMPARE

**宏原型**：

```c
#define __HAL_TIM_SET_COMPARE(__HANDLE__, __CHANNEL__, __COMPARE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）
- `__COMPARE__`：要设置的比较值

**返回值**：无

**作用**：设置定时器指定通道的比较值，通过写入TIM_CCRx寄存器

#### __HAL_TIM_GET_COMPARE

**宏原型**：

```c
#define __HAL_TIM_GET_COMPARE(__HANDLE__, __CHANNEL__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：当前比较值

**作用**：获取定时器指定通道的当前比较值，通过读取TIM_CCRx寄存器

### 1.8 输出比较控制宏

#### __HAL_TIM_ENABLE_OCxPRELOAD

**宏原型**：

```c
#define __HAL_TIM_ENABLE_OCxPRELOAD(__HANDLE__, __CHANNEL__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：无

**作用**：使能定时器指定通道的输出比较预装载，通过设置TIM_CCMRx寄存器的OCxPE位

#### __HAL_TIM_DISABLE_OCxPRELOAD

**宏原型**：

```c
#define __HAL_TIM_DISABLE_OCxPRELOAD(__HANDLE__, __CHANNEL__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：无

**作用**：禁用定时器指定通道的输出比较预装载，通过清除TIM_CCMRx寄存器的OCxPE位

#### __HAL_TIM_ENABLE_OCxFAST

**宏原型**：

```c
#define __HAL_TIM_ENABLE_OCxFAST(__HANDLE__, __CHANNEL__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：无

**作用**：使能定时器指定通道的输出比较快速模式，通过设置TIM_CCMRx寄存器的OCxFE位

#### __HAL_TIM_DISABLE_OCxFAST

**宏原型**：

```c
#define __HAL_TIM_DISABLE_OCxFAST(__HANDLE__, __CHANNEL__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：无

**作用**：禁用定时器指定通道的输出比较快速模式，通过清除TIM_CCMRx寄存器的OCxFE位

### 1.9 更新请求源控制宏

#### __HAL_TIM_URS_ENABLE

**宏原型**：

```c
#define __HAL_TIM_URS_ENABLE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：使能更新请求源，通过设置TIM_CR1寄存器的URS位。当URS位被设置时，只有计数器上溢/下溢才能生成更新中断或DMA请求

#### __HAL_TIM_URS_DISABLE

**宏原型**：

```c
#define __HAL_TIM_URS_DISABLE(__HANDLE__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针

**返回值**：无

**作用**：禁用更新请求源，通过清除TIM_CR1寄存器的URS位。当URS位被清除时，计数器上溢/下溢、设置UG位、通过从模式控制器的更新都能生成更新中断或DMA请求

### 1.10 捕获极性控制宏

#### __HAL_TIM_SET_CAPTUREPOLARITY

**宏原型**：

```c
#define __HAL_TIM_SET_CAPTUREPOLARITY(__HANDLE__, __CHANNEL__, __POLARITY__)
```

**参数**：

- `__HANDLE__`：TIM句柄指针
- `__CHANNEL__`：通道（TIM_CHANNEL_1/2/3/4）
- `__POLARITY__`：要设置的极性
  - TIM_INPUTCHANNELPOLARITY_RISING：上升沿
  - TIM_INPUTCHANNELPOLARITY_FALLING：下降沿
  - TIM_INPUTCHANNELPOLARITY_BOTHEDGE：上升沿和下降沿

**返回值**：无

**作用**：设置定时器指定通道的捕获极性，通过配置TIM_CCER寄存器的CCxP和CCxNP位

## 2. 定时器 HAL 函数

### 2.1 时基单元初始化和启动函数

#### HAL_TIM_Base_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Init(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：初始化定时器的时基单元

#### HAL_TIM_Base_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_DeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：取消定时器的时基单元初始化

#### HAL_TIM_Base_MspInit

**函数原型**：

```c
void HAL_TIM_Base_MspInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：初始化定时器的MSP（MCU Specific Package），这是一个弱函数，用户需要在应用程序中重新实现。通常用于配置定时器相关的时钟、GPIO、中断和DMA等资源。

#### HAL_TIM_Base_MspDeInit

**函数原型**：

```c
void HAL_TIM_Base_MspDeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：取消定时器MSP的初始化，这是一个弱函数，用户需要在应用程序中重新实现。用于释放定时器相关的资源。

#### HAL_TIM_Base_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Start(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：启动定时器的时基单元，使能计数器

#### HAL_TIM_Base_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Stop(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：停止定时器的时基单元，禁用计数器

#### HAL_TIM_Base_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Start_IT(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：启动定时器的时基单元并使能更新中断

#### HAL_TIM_Base_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Stop_IT(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：停止定时器的时基单元并禁用更新中断

#### HAL_TIM_Base_Start_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Start_DMA(TIM_HandleTypeDef *htim, uint32_t *pData, uint16_t Length);
```

**参数**：

- `htim`：TIM句柄指针
- `pData`：指向数据缓冲区的指针
- `Length`：数据缓冲区长度

**返回值**：HAL状态

**作用**：启动定时器的时基单元并使能DMA请求

#### HAL_TIM_Base_Stop_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Base_Stop_DMA(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：停止定时器的时基单元并禁用DMA请求

### 2.2 输出比较初始化和启动函数

#### HAL_TIM_OC_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Init(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：初始化定时器的输出比较模式

#### HAL_TIM_OC_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_DeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：取消定时器的输出比较模式初始化

#### HAL_TIM_OC_MspInit

**函数原型**：

```c
void HAL_TIM_OC_MspInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：初始化定时器输出比较模式的MSP，这是一个弱函数，用户需要在应用程序中重新实现。通常用于配置输出比较模式所需的GPIO、中断等资源。

#### HAL_TIM_OC_MspDeInit

**函数原型**：

```c
void HAL_TIM_OC_MspDeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：取消定时器输出比较模式MSP的初始化，这是一个弱函数，用户需要在应用程序中重新实现。用于释放输出比较模式相关的资源。

#### HAL_TIM_OC_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Start(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：启动定时器的输出比较信号生成

#### HAL_TIM_OC_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Stop(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的输出比较信号生成

#### HAL_TIM_OC_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Start_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：启动定时器的输出比较信号生成并使能相应的中断

#### HAL_TIM_OC_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Stop_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的输出比较信号生成并禁用相应的中断

#### HAL_TIM_OC_Start_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Start_DMA(TIM_HandleTypeDef *htim, uint32_t Channel, uint32_t *pData, uint16_t Length);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）
- `pData`：指向数据缓冲区的指针
- `Length`：数据缓冲区长度

**返回值**：HAL状态

**作用**：启动定时器的输出比较信号生成并使能DMA传输

#### HAL_TIM_OC_Stop_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_Stop_DMA(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的输出比较信号生成并禁用DMA传输

### 2.3 PWM初始化和启动函数

#### HAL_TIM_PWM_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Init(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：初始化定时器的PWM模式

#### HAL_TIM_PWM_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_DeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：取消定时器的PWM模式初始化

#### HAL_TIM_PWM_MspInit

**函数原型**：

```c
void HAL_TIM_PWM_MspInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：初始化定时器PWM模式的MSP，这是一个弱函数，用户需要在应用程序中重新实现。通常用于配置PWM模式所需的GPIO、中断等资源。

#### HAL_TIM_PWM_MspDeInit

**函数原型**：

```c
void HAL_TIM_PWM_MspDeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：取消定时器PWM模式MSP的初始化，这是一个弱函数，用户需要在应用程序中重新实现。用于释放PWM模式相关的资源。

#### HAL_TIM_PWM_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Start(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：启动定时器的PWM信号生成

#### HAL_TIM_PWM_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Stop(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的PWM信号生成

#### HAL_TIM_PWM_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Start_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：启动定时器的PWM信号生成并使能相应的中断

#### HAL_TIM_PWM_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Stop_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的PWM信号生成并禁用相应的中断

#### HAL_TIM_PWM_Start_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Start_DMA(TIM_HandleTypeDef *htim, uint32_t Channel, uint32_t *pData, uint16_t Length);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）
- `pData`：指向数据缓冲区的指针
- `Length`：数据缓冲区长度

**返回值**：HAL状态

**作用**：启动定时器的PWM信号生成并使能DMA传输

#### HAL_TIM_PWM_Stop_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_Stop_DMA(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的PWM信号生成并禁用DMA传输

### 2.4 输入捕获初始化和启动函数

#### HAL_TIM_IC_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Init(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：初始化定时器的输入捕获模式

#### HAL_TIM_IC_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_DeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：取消定时器的输入捕获模式初始化

#### HAL_TIM_IC_MspInit

**函数原型**：

```c
void HAL_TIM_IC_MspInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：初始化定时器输入捕获模式的MSP，这是一个弱函数，用户需要在应用程序中重新实现。通常用于配置输入捕获模式所需的GPIO、中断等资源。

#### HAL_TIM_IC_MspDeInit

**函数原型**：

```c
void HAL_TIM_IC_MspDeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：取消定时器输入捕获模式MSP的初始化，这是一个弱函数，用户需要在应用程序中重新实现。用于释放输入捕获模式相关的资源。

#### HAL_TIM_IC_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Start(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：启动定时器的输入捕获

#### HAL_TIM_IC_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Stop(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的输入捕获

#### HAL_TIM_IC_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Start_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：启动定时器的输入捕获并使能相应的中断

#### HAL_TIM_IC_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Stop_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的输入捕获并禁用相应的中断

#### HAL_TIM_IC_Start_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Start_DMA(TIM_HandleTypeDef *htim, uint32_t Channel, uint32_t *pData, uint16_t Length);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）
- `pData`：指向数据缓冲区的指针
- `Length`：数据缓冲区长度

**返回值**：HAL状态

**作用**：启动定时器的输入捕获并使能DMA传输

#### HAL_TIM_IC_Stop_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_Stop_DMA(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：停止定时器的输入捕获并禁用DMA传输

### 2.5 单脉冲初始化和启动函数

#### HAL_TIM_OnePulse_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_Init(TIM_HandleTypeDef *htim, uint32_t OnePulseMode);
```

**参数**：

- `htim`：TIM句柄指针
- `OnePulseMode`：单脉冲模式（TIM_OPMODE_SINGLE或TIM_OPMODE_REPETITIVE）

**返回值**：HAL状态

**作用**：初始化定时器的单脉冲模式

#### HAL_TIM_OnePulse_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_DeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：取消定时器的单脉冲模式初始化

#### HAL_TIM_OnePulse_MspInit

**函数原型**：

```c
void HAL_TIM_OnePulse_MspInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：初始化定时器单脉冲模式的MSP，这是一个弱函数，用户需要在应用程序中重新实现。通常用于配置单脉冲模式所需的GPIO、中断等资源。

#### HAL_TIM_OnePulse_MspDeInit

**函数原型**：

```c
void HAL_TIM_OnePulse_MspDeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：取消定时器单脉冲模式MSP的初始化，这是一个弱函数，用户需要在应用程序中重新实现。用于释放单脉冲模式相关的资源。

#### HAL_TIM_OnePulse_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_Start(TIM_HandleTypeDef *htim, uint32_t OutputChannel);
```

**参数**：

- `htim`：TIM句柄指针
- `OutputChannel`：输出通道（TIM_CHANNEL_1或TIM_CHANNEL_2）

**返回值**：HAL状态

**作用**：启动定时器的单脉冲模式

#### HAL_TIM_OnePulse_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_Stop(TIM_HandleTypeDef *htim, uint32_t OutputChannel);
```

**参数**：

- `htim`：TIM句柄指针
- `OutputChannel`：输出通道（TIM_CHANNEL_1或TIM_CHANNEL_2）

**返回值**：HAL状态

**作用**：停止定时器的单脉冲模式

#### HAL_TIM_OnePulse_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_Start_IT(TIM_HandleTypeDef *htim, uint32_t OutputChannel);
```

**参数**：

- `htim`：TIM句柄指针
- `OutputChannel`：输出通道（TIM_CHANNEL_1或TIM_CHANNEL_2）

**返回值**：HAL状态

**作用**：启动定时器的单脉冲模式并使能相应的中断

#### HAL_TIM_OnePulse_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_Stop_IT(TIM_HandleTypeDef *htim, uint32_t OutputChannel);
```

**参数**：

- `htim`：TIM句柄指针
- `OutputChannel`：输出通道（TIM_CHANNEL_1或TIM_CHANNEL_2）

**返回值**：HAL状态

**作用**：停止定时器的单脉冲模式并禁用相应的中断

### 2.6 编码器初始化和启动函数

#### HAL_TIM_Encoder_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Init(TIM_HandleTypeDef *htim, TIM_Encoder_InitTypeDef *sConfig);
```

**参数**：

- `htim`：TIM句柄指针
- `sConfig`：编码器接口配置结构体指针

**返回值**：HAL状态

**作用**：初始化定时器的编码器接口模式

#### HAL_TIM_Encoder_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_DeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：取消定时器的编码器接口模式初始化

#### HAL_TIM_Encoder_MspInit

**函数原型**：

```c
void HAL_TIM_Encoder_MspInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：初始化定时器编码器接口模式的MSP，这是一个弱函数，用户需要在应用程序中重新实现。通常用于配置编码器接口模式所需的GPIO、中断等资源。

#### HAL_TIM_Encoder_MspDeInit

**函数原型**：

```c
void HAL_TIM_Encoder_MspDeInit(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：取消定时器编码器接口模式MSP的初始化，这是一个弱函数，用户需要在应用程序中重新实现。用于释放编码器接口模式相关的资源。

#### HAL_TIM_Encoder_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Start(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_ALL）

**返回值**：HAL状态

**作用**：启动定时器的编码器接口

#### HAL_TIM_Encoder_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Stop(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_ALL）

**返回值**：HAL状态

**作用**：停止定时器的编码器接口

#### HAL_TIM_Encoder_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Start_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_ALL）

**返回值**：HAL状态

**作用**：启动定时器的编码器接口并使能相应的中断

#### HAL_TIM_Encoder_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Stop_IT(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_ALL）

**返回值**：HAL状态

**作用**：停止定时器的编码器接口并禁用相应的中断

#### HAL_TIM_Encoder_Start_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Start_DMA(TIM_HandleTypeDef *htim, uint32_t Channel, uint32_t *pData1, uint32_t *pData2, uint16_t Length);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/ALL）
- `pData1`：指向通道1数据缓冲区的指针
- `pData2`：指向通道2数据缓冲区的指针
- `Length`：数据缓冲区长度

**返回值**：HAL状态

**作用**：启动定时器的编码器接口并使能DMA传输

#### HAL_TIM_Encoder_Stop_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_Encoder_Stop_DMA(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/ALL）

**返回值**：HAL状态

**作用**：停止定时器的编码器接口并禁用DMA传输

### 2.7 从模式配置函数

#### HAL_TIM_SlaveConfigSynchro

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_SlaveConfigSynchro(TIM_HandleTypeDef *htim, TIM_SlaveConfigTypeDef *sSlaveConfig);
```

**参数**：

- `htim`：TIM句柄指针
- `sSlaveConfig`：从模式配置结构体指针

**返回值**：HAL状态

**作用**：配置定时器的从模式同步

#### HAL_TIM_SlaveConfigSynchro_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_SlaveConfigSynchro_IT(TIM_HandleTypeDef *htim, TIM_SlaveConfigTypeDef *sSlaveConfig);
```

**参数**：

- `htim`：TIM句柄指针
- `sSlaveConfig`：从模式配置结构体指针

**返回值**：HAL状态

**作用**：配置定时器的从模式同步并使能触发中断

### 2.8 通道配置函数

#### HAL_TIM_OC_ConfigChannel

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OC_ConfigChannel(TIM_HandleTypeDef *htim, TIM_OC_InitTypeDef *sConfig, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `sConfig`：输出比较配置结构体指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：配置定时器的输出比较通道

#### HAL_TIM_PWM_ConfigChannel

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_PWM_ConfigChannel(TIM_HandleTypeDef *htim, TIM_OC_InitTypeDef *sConfig, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `sConfig`：PWM配置结构体指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：配置定时器的PWM通道

#### HAL_TIM_IC_ConfigChannel

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_IC_ConfigChannel(TIM_HandleTypeDef *htim, TIM_IC_InitTypeDef *sConfig, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `sConfig`：输入捕获配置结构体指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：配置定时器的输入捕获通道

#### HAL_TIM_OnePulse_ConfigChannel

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_OnePulse_ConfigChannel(TIM_HandleTypeDef *htim, uint32_t OCMode, uint32_t OCPolarity, uint32_t OCNPolarity, uint32_t OCIdleState, uint32_t OCNIdleState, uint32_t ICPolarity, uint32_t ICSelection, uint32_t ICFilter, uint32_t Channel, uint32_t OffStateRunMode, uint32_t OffStateIDLEMode);
```

**参数**：

- `htim`：TIM句柄指针
- `OCMode`：输出比较模式
- `OCPolarity`：输出比较极性
- `OCNPolarity`：互补输出比较极性
- `OCIdleState`：输出比较空闲状态
- `OCNIdleState`：互补输出比较空闲状态
- `ICPolarity`：输入捕获极性
- `ICSelection`：输入捕获选择
- `ICFilter`：输入捕获滤波器
- `Channel`：通道（TIM_CHANNEL_1/2）
- `OffStateRunMode`：运行模式下的关闭状态
- `OffStateIDLEMode`：空闲模式下的关闭状态

**返回值**：HAL状态

**作用**：配置定时器的单脉冲通道

### 2.9 中断处理函数

#### HAL_TIM_IRQHandler

**函数原型**：

```c
void HAL_TIM_IRQHandler(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：处理定时器中断请求，调用相应的回调函数

### 2.10 回调函数

#### HAL_TIM_PeriodElapsedCallback

**函数原型**：

```c
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器周期溢出回调函数，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_PeriodElapsedHalfCpltCallback

**函数原型**：

```c
void HAL_TIM_PeriodElapsedHalfCpltCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器周期溢出半完成回调函数，用于DMA传输半完成时的回调，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_OC_DelayElapsedCallback

**函数原型**：

```c
void HAL_TIM_OC_DelayElapsedCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器输出比较延迟完成回调函数，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_IC_CaptureCallback

**函数原型**：

```c
void HAL_TIM_IC_CaptureCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器输入捕获完成回调函数，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_IC_CaptureHalfCpltCallback

**函数原型**：

```c
void HAL_TIM_IC_CaptureHalfCpltCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器输入捕获半完成回调函数，用于DMA传输半完成时的回调，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_PWM_PulseFinishedCallback

**函数原型**：

```c
void HAL_TIM_PWM_PulseFinishedCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器PWM脉冲完成回调函数，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_PWM_PulseFinishedHalfCpltCallback

**函数原型**：

```c
void HAL_TIM_PWM_PulseFinishedHalfCpltCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器PWM脉冲半完成回调函数，用于DMA传输半完成时的回调，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_TriggerCallback

**函数原型**：

```c
void HAL_TIM_TriggerCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器触发回调函数，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_TriggerHalfCpltCallback

**函数原型**：

```c
void HAL_TIM_TriggerHalfCpltCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器触发半完成回调函数，用于DMA传输半完成时的回调，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_ErrorCallback

**函数原型**：

```c
void HAL_TIM_ErrorCallback(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：无

**作用**：定时器错误回调函数，这是一个弱函数，用户需要在应用程序中重新实现

#### HAL_TIM_RegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_RegisterCallback(TIM_HandleTypeDef *htim, HAL_TIM_CallbackIDTypeDef CallbackID, pTIM_CallbackTypeDef pCallback);
```

**参数**：

- `htim`：TIM句柄指针
- `CallbackID`：回调ID
- `pCallback`：回调函数指针

**返回值**：HAL状态

**作用**：注册定时器回调函数

#### HAL_TIM_UnRegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_UnRegisterCallback(TIM_HandleTypeDef *htim, HAL_TIM_CallbackIDTypeDef CallbackID);
```

**参数**：

- `htim`：TIM句柄指针
- `CallbackID`：回调ID

**返回值**：HAL状态

**作用**：注销定时器回调函数



### 2.11 操作函数

#### HAL_TIM_ConfigOCrefClear

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_ConfigOCrefClear(TIM_HandleTypeDef *htim, TIM_ClearInputConfigTypeDef *sClearConfig, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `sClearConfig`：清除配置结构体指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：HAL状态

**作用**：配置定时器的OCRef清除功能

#### HAL_TIM_ConfigClockSource

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_ConfigClockSource(TIM_HandleTypeDef *htim, TIM_ClockConfigTypeDef *sClockSourceConfig);
```

**参数**：

- `htim`：TIM句柄指针
- `sClockSourceConfig`：时钟源配置结构体指针

**返回值**：HAL状态

**作用**：配置定时器的时钟源

#### HAL_TIM_ConfigTI1Input

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_ConfigTI1Input(TIM_HandleTypeDef *htim, uint32_t TI1_Selection);
```

**参数**：

- `htim`：TIM句柄指针
- `TI1_Selection`：TI1输入选择

**返回值**：HAL状态

**作用**：配置定时器的TI1输入



#### HAL_TIM_DMABurst_WriteStart

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_DMABurst_WriteStart(TIM_HandleTypeDef *htim, uint32_t BurstBaseAddress, uint32_t BurstRequestSrc, uint32_t *BurstBuffer, uint32_t BurstLength);
```

**参数**：

- `htim`：TIM句柄指针
- `BurstBaseAddress`：DMA突发基地址
- `BurstRequestSrc`：DMA突发请求源
- `BurstBuffer`：指向DMA突发缓冲区的指针
- `BurstLength`：DMA突发长度

**返回值**：HAL状态

**作用**：启动定时器的DMA突发写操作

#### HAL_TIM_DMABurst_MultiWriteStart

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_DMABurst_MultiWriteStart(TIM_HandleTypeDef *htim, uint32_t BurstBaseAddress, uint32_t BurstRequestSrc, uint32_t *BurstBuffer, uint32_t BurstLength, uint32_t DataLength);
```

**参数**：

- `htim`：TIM句柄指针
- `BurstBaseAddress`：DMA突发基地址
- `BurstRequestSrc`：DMA突发请求源
- `BurstBuffer`：指向DMA突发缓冲区的指针
- `BurstLength`：DMA突发长度
- `DataLength`：数据长度

**返回值**：HAL状态

**作用**：启动定时器的DMA多次突发写操作

#### HAL_TIM_DMABurst_WriteStop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_DMABurst_WriteStop(TIM_HandleTypeDef *htim, uint32_t BurstRequestSrc);
```

**参数**：

- `htim`：TIM句柄指针
- `BurstRequestSrc`：DMA突发请求源

**返回值**：HAL状态

**作用**：停止定时器的DMA突发写操作

#### HAL_TIM_DMABurst_ReadStart

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_DMABurst_ReadStart(TIM_HandleTypeDef *htim, uint32_t BurstBaseAddress, uint32_t BurstRequestSrc, uint32_t *BurstBuffer, uint32_t BurstLength);
```

**参数**：

- `htim`：TIM句柄指针
- `BurstBaseAddress`：DMA突发基地址
- `BurstRequestSrc`：DMA突发请求源
- `BurstBuffer`：指向DMA突发缓冲区的指针
- `BurstLength`：DMA突发长度

**返回值**：HAL状态

**作用**：启动定时器的DMA突发读操作

#### HAL_TIM_DMABurst_MultiReadStart

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_DMABurst_MultiReadStart(TIM_HandleTypeDef *htim, uint32_t BurstBaseAddress, uint32_t BurstRequestSrc, uint32_t *BurstBuffer, uint32_t BurstLength, uint32_t DataLength);
```

**参数**：

- `htim`：TIM句柄指针
- `BurstBaseAddress`：DMA突发基地址
- `BurstRequestSrc`：DMA突发请求源
- `BurstBuffer`：指向DMA突发缓冲区的指针
- `BurstLength`：DMA突发长度
- `DataLength`：数据长度

**返回值**：HAL状态

**作用**：启动定时器的DMA多次突发读操作

#### HAL_TIM_DMABurst_ReadStop

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_DMABurst_ReadStop(TIM_HandleTypeDef *htim, uint32_t BurstRequestSrc);
```

**参数**：

- `htim`：TIM句柄指针
- `BurstRequestSrc`：DMA突发请求源

**返回值**：HAL状态

**作用**：停止定时器的DMA突发读操作

#### HAL_TIM_GenerateEvent

**函数原型**：

```c
HAL_StatusTypeDef HAL_TIM_GenerateEvent(TIM_HandleTypeDef *htim, uint32_t EventSource);
```

**参数**：

- `htim`：TIM句柄指针
- `EventSource`：事件源

**返回值**：HAL状态

**作用**：生成定时器事件

#### HAL_TIM_ReadCapturedValue

**函数原型**：

```c
uint32_t HAL_TIM_ReadCapturedValue(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：捕获值

**作用**：读取定时器的捕获值

### 2.12 状态获取函数

#### HAL_TIM_Base_GetState

**函数原型**：

```c
HAL_TIM_StateTypeDef HAL_TIM_Base_GetState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：获取定时器基本模式的状态

#### HAL_TIM_OC_GetState

**函数原型**：

```c
HAL_TIM_StateTypeDef HAL_TIM_OC_GetState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：获取定时器输出比较模式的状态

#### HAL_TIM_PWM_GetState

**函数原型**：

```c
HAL_TIM_StateTypeDef HAL_TIM_PWM_GetState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：获取定时器PWM模式的状态

#### HAL_TIM_IC_GetState

**函数原型**：

```c
HAL_TIM_StateTypeDef HAL_TIM_IC_GetState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：获取定时器输入捕获模式的状态

#### HAL_TIM_OnePulse_GetState

**函数原型**：

```c
HAL_TIM_StateTypeDef HAL_TIM_OnePulse_GetState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：获取定时器单脉冲模式的状态

#### HAL_TIM_Encoder_GetState

**函数原型**：

```c
HAL_TIM_StateTypeDef HAL_TIM_Encoder_GetState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：HAL状态

**作用**：获取定时器编码器模式的状态

#### HAL_TIM_GetActiveChannel

**函数原型**：

```c
HAL_TIM_ActiveChannel HAL_TIM_GetActiveChannel(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：活动通道

**作用**：获取定时器的活动通道

#### HAL_TIM_GetChannelState

**函数原型**：

```c
HAL_TIM_ChannelStateTypeDef HAL_TIM_GetChannelState(TIM_HandleTypeDef *htim, uint32_t Channel);
```

**参数**：

- `htim`：TIM句柄指针
- `Channel`：通道（TIM_CHANNEL_1/2/3/4）

**返回值**：通道状态

**作用**：获取定时器指定通道的状态

#### HAL_TIM_DMABurstState

**函数原型**：

```c
HAL_TIM_DMABurstStateTypeDef HAL_TIM_DMABurstState(TIM_HandleTypeDef *htim);
```

**参数**：

- `htim`：TIM句柄指针

**返回值**：DMA突发状态

**作用**：获取定时器DMA突发的状态

