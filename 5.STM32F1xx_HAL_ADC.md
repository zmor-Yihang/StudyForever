# STM32F1xx HAL ADC库函数说明文档

本文档介绍了STM32F1xx HAL库中的ADC函数，包括函数原型、参数、返回值和具体作用。

## 1. ADC __HAL 宏函数

### __HAL_ADC_ENABLE

**宏原型**：
```c
#define __HAL_ADC_ENABLE(__HANDLE__) (SET_BIT((__HANDLE__)->Instance->CR2, (ADC_CR2_ADON)))
```

**参数**：
- `__HANDLE__`：ADC句柄

**返回值**：无

**作用**：启用ADC外设。注意：ADC启用需要一段稳定时间(参见设备数据手册中的tSTAB参数)。在STM32F1上，如果ADC已经启用，此宏将触发常规组的软件转换开始。

### __HAL_ADC_DISABLE

**宏原型**：
```c
#define __HAL_ADC_DISABLE(__HANDLE__) (CLEAR_BIT((__HANDLE__)->Instance->CR2, (ADC_CR2_ADON)))
```

**参数**：
- `__HANDLE__`：ADC句柄

**返回值**：无

**作用**：禁用ADC外设。

### __HAL_ADC_ENABLE_IT

**宏原型**：
```c
#define __HAL_ADC_ENABLE_IT(__HANDLE__, __INTERRUPT__) (SET_BIT((__HANDLE__)->Instance->CR1, (__INTERRUPT__)))
```

**参数**：
- `__HANDLE__`：ADC句柄
- `__INTERRUPT__`：ADC中断，可以是以下值的组合：
  - `ADC_IT_EOC`：ADC常规转换结束中断源
  - `ADC_IT_JEOC`：ADC注入转换结束中断源
  - `ADC_IT_AWD`：ADC模拟看门狗中断源

**返回值**：无

**作用**：启用ADC转换结束中断。

### __HAL_ADC_DISABLE_IT

**宏原型**：
```c
#define __HAL_ADC_DISABLE_IT(__HANDLE__, __INTERRUPT__) (CLEAR_BIT((__HANDLE__)->Instance->CR1, (__INTERRUPT__)))
```

**参数**：
- `__HANDLE__`：ADC句柄
- `__INTERRUPT__`：ADC中断，可以是以下值的组合：
  - `ADC_IT_EOC`：ADC常规转换结束中断源
  - `ADC_IT_JEOC`：ADC注入转换结束中断源
  - `ADC_IT_AWD`：ADC模拟看门狗中断源

**返回值**：无

**作用**：禁用ADC转换结束中断。

### __HAL_ADC_GET_IT_SOURCE

**宏原型**：
```c
#define __HAL_ADC_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) (((__HANDLE__)->Instance->CR1 & (__INTERRUPT__)) == (__INTERRUPT__))
```

**参数**：
- `__HANDLE__`：ADC句柄
- `__INTERRUPT__`：要检查的ADC中断源

**返回值**：
- 返回指定ADC中断源的状态（已启用或已禁用）

**作用**：检查指定的ADC中断源是否启用或禁用。

### __HAL_ADC_GET_FLAG

**宏原型**：
```c
#define __HAL_ADC_GET_FLAG(__HANDLE__, __FLAG__) ((((__HANDLE__)->Instance->SR) & (__FLAG__)) == (__FLAG__))
```

**参数**：
- `__HANDLE__`：ADC句柄
- `__FLAG__`：ADC标志，可以是以下值的组合：
  - `ADC_FLAG_STRT`：ADC常规组开始标志
  - `ADC_FLAG_JSTRT`：ADC注入组开始标志
  - `ADC_FLAG_EOC`：ADC常规转换结束标志
  - `ADC_FLAG_JEOC`：ADC注入转换结束标志
  - `ADC_FLAG_AWD`：ADC模拟看门狗标志

**返回值**：
- 返回选定ADC标志的状态（SET或RESET）

**作用**：获取选定的ADC标志状态。

### __HAL_ADC_CLEAR_FLAG

**宏原型**：
```c
#define __HAL_ADC_CLEAR_FLAG(__HANDLE__, __FLAG__) (WRITE_REG((__HANDLE__)->Instance->SR, ~(__FLAG__)))
```

**参数**：
- `__HANDLE__`：ADC句柄
- `__FLAG__`：ADC标志，可以是以下值的组合：
  - `ADC_FLAG_STRT`：ADC常规组开始标志
  - `ADC_FLAG_JSTRT`：ADC注入组开始标志
  - `ADC_FLAG_EOC`：ADC常规转换结束标志
  - `ADC_FLAG_JEOC`：ADC注入转换结束标志
  - `ADC_FLAG_AWD`：ADC模拟看门狗标志

**返回值**：无

**作用**：清除ADC的挂起标志。

### __HAL_ADC_RESET_HANDLE_STATE

**宏原型**：
```c
#define __HAL_ADC_RESET_HANDLE_STATE(__HANDLE__) ((__HANDLE__)->State = HAL_ADC_STATE_RESET)
```

**参数**：
- `__HANDLE__`：ADC句柄

**返回值**：无

**作用**：重置ADC句柄状态。

## 2. ADC初始化函数

### HAL_ADC_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADC_Init(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：指向ADC_HandleTypeDef结构的指针，该结构包含ADC实例的配置信息

**返回值**：
- HAL状态

**作用**：根据ADC_InitTypeDef结构中指定的参数初始化ADC外设和常规组。

### HAL_ADC_DeInit

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADC_DeInit(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：指向ADC_HandleTypeDef结构的指针，该结构包含ADC实例的配置信息

**返回值**：
- HAL状态

**作用**：将ADC外设寄存器重置为其默认复位值，并进行ADC MSP的反初始化。

### HAL_ADC_MspInit

**函数原型**：
```c
void HAL_ADC_MspInit(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：指向ADC_HandleTypeDef结构的指针，该结构包含ADC实例的配置信息

**返回值**：无

**作用**：初始化ADC的MSP（MCU Support Package，如GPIO、时钟等）。这是一个弱函数，应该由用户在他们的代码中实现。

### HAL_ADC_MspDeInit

**函数原型**：
```c
void HAL_ADC_MspDeInit(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：指向ADC_HandleTypeDef结构的指针，该结构包含ADC实例的配置信息

**返回值**：无

**作用**：反初始化ADC的MSP。这是一个弱函数，应该由用户在他们的代码中实现。

## 3. ADC通道配置函数

### HAL_ADC_ConfigChannel

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_ConfigChannel(ADC_HandleTypeDef* hadc, ADC_ChannelConfTypeDef* sConfig);
```

**参数**：

- `hadc`：ADC句柄
- `sConfig`：ADC常规组通道的结构

**返回值**：

- HAL状态

**作用**：配置选定的通道链接到常规组。

### HAL_ADC_AnalogWDGConfig

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_AnalogWDGConfig(ADC_HandleTypeDef* hadc, ADC_AnalogWDGConfTypeDef* AnalogWDGConfig);
```

**参数**：

- `hadc`：ADC句柄
- `AnalogWDGConfig`：ADC模拟看门狗配置结构

**返回值**：

- HAL状态

**作用**：配置模拟看门狗。模拟看门狗阈值可以在ADC转换进行时修改。

## 4. ADC启动函数

### HAL_ADC_Start

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_Start(ADC_HandleTypeDef* hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- HAL状态

**作用**：启用ADC，开始常规组转换。此函数不启用任何中断。

### HAL_ADC_Stop

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_Stop(ADC_HandleTypeDef* hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- HAL状态

**作用**：停止常规组转换（以及在自动注入模式下的注入组转换），禁用ADC外设。

### HAL_ADC_Start_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_Start_IT(ADC_HandleTypeDef* hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- HAL状态

**作用**：启用ADC，开始常规组转换，并启用中断。启用的中断为EOC（常规组转换结束）。

### HAL_ADC_Stop_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_Stop_IT(ADC_HandleTypeDef* hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- HAL状态

**作用**：停止常规组转换（以及在自动注入模式下的注入组转换），禁用转换结束中断，禁用ADC外设。

### HAL_ADC_Start_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_Start_DMA(ADC_HandleTypeDef* hadc, uint32_t* pData, uint32_t Length);
```

**参数**：

- `hadc`：ADC句柄
- `pData`：目标缓冲区地址
- `Length`：从ADC外设传输到内存的数据长度

**返回值**：

- HAL状态

**作用**：启用ADC，开始常规组转换，并通过DMA传输结果。

### HAL_ADC_Stop_DMA

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_Stop_DMA(ADC_HandleTypeDef* hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- HAL状态

**作用**：停止常规组转换，禁用ADC DMA传输，禁用ADC外设。

## 5. ADC操作函数

### HAL_ADC_PollForConversion

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADC_PollForConversion(ADC_HandleTypeDef* hadc, uint32_t Timeout);
```

**参数**：
- `hadc`：ADC句柄
- `Timeout`：超时值（以毫秒为单位）

**返回值**：
- HAL状态

**作用**：等待规则组转换完成。此函数不能在DMA模式下使用。

### HAL_ADC_PollForEvent

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADC_PollForEvent(ADC_HandleTypeDef* hadc, uint32_t EventType, uint32_t Timeout);
```

**参数**：
- `hadc`：ADC句柄
- `EventType`：ADC事件类型，可以是`ADC_AWD_EVENT`（ADC模拟看门狗事件）
- `Timeout`：超时值（以毫秒为单位）

**返回值**：
- HAL状态

**作用**：轮询转换事件。

### HAL_ADC_GetValue

**函数原型**：
```c
uint32_t HAL_ADC_GetValue(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：
- ADC常规组转换数据

**作用**：获取ADC常规组转换结果。读取DR寄存器会自动清除ADC EOC标志。

## 6. ADC状态函数

### HAL_ADC_GetState

**函数原型**：

```c
uint32_t HAL_ADC_GetState(ADC_HandleTypeDef* hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- ADC状态

**作用**：返回ADC状态。

### HAL_ADC_GetError

**函数原型**：

```c
uint32_t HAL_ADC_GetError(ADC_HandleTypeDef *hadc);
```

**参数**：

- `hadc`：ADC句柄

**返回值**：

- ADC错误代码

**作用**：返回ADC错误代码。

## 7. ADC中断处理函数

### HAL_ADC_IRQHandler

**函数原型**：
```c
void HAL_ADC_IRQHandler(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：无

**作用**：处理ADC中断请求。

## 8. ADC回调函数

### HAL_ADC_ConvCpltCallback

**函数原型**：
```c
void HAL_ADC_ConvCpltCallback(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：无

**作用**：非阻塞模式下的转换完成回调。这是一个弱函数，当需要回调时，必须在用户文件中实现HAL_ADC_ConvCpltCallback函数。

### HAL_ADC_ConvHalfCpltCallback

**函数原型**：
```c
void HAL_ADC_ConvHalfCpltCallback(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：无

**作用**：非阻塞模式下的转换DMA半传输回调。这是一个弱函数，当需要回调时，必须在用户文件中实现HAL_ADC_ConvHalfCpltCallback函数。

### HAL_ADC_LevelOutOfWindowCallback

**函数原型**：
```c
void HAL_ADC_LevelOutOfWindowCallback(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：无

**作用**：非阻塞模式下的模拟看门狗回调。这是一个弱函数，当需要回调时，必须在用户文件中实现HAL_ADC_LevelOutOfWindowCallback函数。

### HAL_ADC_ErrorCallback

**函数原型**：
```c
void HAL_ADC_ErrorCallback(ADC_HandleTypeDef *hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：无

**作用**：非阻塞模式下的ADC错误回调。这是一个弱函数，当需要回调时，必须在用户文件中实现HAL_ADC_ErrorCallback函数。

### HAL_ADC_RegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_RegisterCallback(ADC_HandleTypeDef *hadc, HAL_ADC_CallbackIDTypeDef CallbackID, pADC_CallbackTypeDef pCallback);
```

**参数**：

- `hadc`：ADC句柄
- `CallbackID`：要注册的回调ID
- `pCallback`：指向回调函数的指针

**返回值**：

- HAL状态

**作用**：注册用户ADC回调，用于替代弱预定义回调。注意：此函数仅在USE_HAL_ADC_REGISTER_CALLBACKS设置为1时可用。

### HAL_ADC_UnRegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_ADC_UnRegisterCallback(ADC_HandleTypeDef *hadc, HAL_ADC_CallbackIDTypeDef CallbackID);
```

**参数**：

- `hadc`：ADC句柄
- `CallbackID`：要注销的回调ID

**返回值**：

- HAL状态

**作用**：注销用户ADC回调，恢复为弱预定义回调。注意：此函数仅在USE_HAL_ADC_REGISTER_CALLBACKS设置为1时可用。

## 9. ADC扩展功能函数

### HAL_ADCEx_Calibration_Start

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_Calibration_Start(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：
- HAL状态

**作用**：执行ADC自动校准。校准前提条件：ADC必须禁用（在HAL_ADC_Start()前或HAL_ADC_Stop()后执行此函数）。

### HAL_ADCEx_InjectedStart

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_InjectedStart(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：
- HAL状态

**作用**：启用ADC，开始注入组转换。此函数不启用任何中断。

### HAL_ADCEx_InjectedStop

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_InjectedStop(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：
- HAL状态

**作用**：停止注入通道的转换。如果常规组没有进行转换，则禁用ADC外设。注意：如果需要禁用ADC并且常规组转换正在进行，必须使用HAL_ADC_Stop函数停止注入和常规组，并禁用ADC。在自动注入模式下，必须使用HAL_ADC_Stop函数。

### HAL_ADCEx_InjectedPollForConversion

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_InjectedPollForConversion(ADC_HandleTypeDef* hadc, uint32_t Timeout);
```

**参数**：
- `hadc`：ADC句柄
- `Timeout`：超时值（以毫秒为单位）

**返回值**：
- HAL状态

**作用**：等待注入组转换完成。

### HAL_ADCEx_InjectedStart_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_InjectedStart_IT(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：
- HAL状态

**作用**：启用ADC，开始注入组转换，并启用中断。启用的中断为JEOC（注入组转换结束）。

### HAL_ADCEx_InjectedStop_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_InjectedStop_IT(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：
- HAL状态

**作用**：停止注入通道的转换，禁用转换结束中断，并在没有常规组转换进行时禁用ADC外设。在自动注入模式下，必须使用HAL_ADC_Stop_IT函数。

### HAL_ADCEx_InjectedGetValue

**函数原型**：
```c
uint32_t HAL_ADCEx_InjectedGetValue(ADC_HandleTypeDef* hadc, uint32_t InjectedRank);
```

**参数**：
- `hadc`：ADC句柄
- `InjectedRank`：被转换的ADC注入等级，可以是以下值之一：
  - `ADC_INJECTED_RANK_1`：已选择注入通道1
  - `ADC_INJECTED_RANK_2`：已选择注入通道2
  - `ADC_INJECTED_RANK_3`：已选择注入通道3
  - `ADC_INJECTED_RANK_4`：已选择注入通道4

**返回值**：
- ADC注入组转换数据

**作用**：获取ADC注入组转换结果。读取JDRx寄存器会自动清除ADC JEOC标志，但不会清除JEOS标志。

### HAL_ADCEx_InjectedConfigChannel

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_InjectedConfigChannel(ADC_HandleTypeDef* hadc, ADC_InjectionConfTypeDef* sConfigInjected);
```

**参数**：
- `hadc`：ADC句柄
- `sConfigInjected`：ADC注入组和ADC通道的结构

**返回值**：
- HAL状态

**作用**：配置ADC注入组和选定的通道链接到注入组。此函数可以在ADC不进行转换时动态更新某些参数。

### HAL_ADCEx_InjectedConvCpltCallback

**函数原型**：
```c
void HAL_ADCEx_InjectedConvCpltCallback(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC句柄

**返回值**：无

**作用**：非阻塞模式下的注入转换完成回调。这是一个弱函数，当需要回调时，必须在用户文件中实现HAL_ADCEx_InjectedConvCpltCallback函数。

## 10. ADC多模式功能函数

以下函数仅适用于STM32F103x6/xB、STM32F105xC、STM32F107xC、STM32F103xE/xG设备：

### HAL_ADCEx_MultiModeStart_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_MultiModeStart_DMA(ADC_HandleTypeDef* hadc, uint32_t* pData, uint32_t Length);
```

**参数**：
- `hadc`：ADC主设备的句柄（不能使用ADC从设备的句柄）
- `pData`：目标缓冲区地址
- `Length`：从ADC外设传输到内存的数据长度

**返回值**：
- HAL状态

**作用**：启用ADC，开始常规组转换，并通过DMA传输结果。必须事先使用HAL_ADCEx_MultiModeConfigChannel()函数配置多模式。启用的中断包括DMA传输完成和DMA半传输。注意：在STM32F1设备上，ADC从设备的常规组必须配置为使用ADC_SOFTWARE_START转换触发器。ADC从设备可以使用单模式HAL_ADC_Start()函数提前启用。

### HAL_ADCEx_MultiModeStop_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_MultiModeStop_DMA(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC主设备的句柄（不能使用ADC从设备的句柄）

**返回值**：
- HAL状态

**作用**：停止常规组转换，禁用ADC DMA传输，禁用ADC外设。注意：多模式在此函数后仍保持启用。要禁用多模式，必须使用HAL_ADC_Init()或HAL_ADC_ReInit()重新初始化ADC。如果DMA配置为循环模式，在调用此函数后必须使用ADC从设备的句柄调用HAL_ADC_Stop_DMA函数，以正确禁用DMA通道。

### HAL_ADCEx_MultiModeGetValue

**函数原型**：
```c
uint32_t HAL_ADCEx_MultiModeGetValue(ADC_HandleTypeDef* hadc);
```

**参数**：
- `hadc`：ADC主设备的句柄（不能使用ADC从设备的句柄）

**返回值**：
- 转换数据值

**作用**：返回最后的ADC主设备和从设备常规转换结果数据，在选定的多模式中。

### HAL_ADCEx_MultiModeConfigChannel

**函数原型**：
```c
HAL_StatusTypeDef HAL_ADCEx_MultiModeConfigChannel(ADC_HandleTypeDef* hadc, ADC_MultiModeTypeDef* multimode);
```

**参数**：
- `hadc`：ADC句柄
- `multimode`：ADC多模式配置结构

**返回值**：
- HAL状态

**作用**：启用ADC多模式并配置多模式参数。此函数可以动态更新某些参数，但设置这些参数的条件是：共同组中的两个ADC都必须处于禁用状态。注意：要将配置从多模式更改回单模式，必须重置ADC（使用HAL_ADC_Init()函数）。



