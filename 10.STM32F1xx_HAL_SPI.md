# STM32F1xx HAL SPI库函数说明文档

本文档介绍了STM32F1xx HAL库中的SPI函数，包括函数原型、参数、返回值和具体作用。

## 1. __HAL 宏函数

### __HAL_SPI_RESET_HANDLE_STATE

**宏原型**：
```c
#if (USE_HAL_SPI_REGISTER_CALLBACKS == 1U)
#define __HAL_SPI_RESET_HANDLE_STATE(__HANDLE__)                do{                                                  \
                                                                    (__HANDLE__)->State = HAL_SPI_STATE_RESET;       \
                                                                    (__HANDLE__)->MspInitCallback = NULL;            \
                                                                    (__HANDLE__)->MspDeInitCallback = NULL;          \
                                                                  } while(0)
#else
#define __HAL_SPI_RESET_HANDLE_STATE(__HANDLE__) ((__HANDLE__)->State = HAL_SPI_STATE_RESET)
#endif /* USE_HAL_SPI_REGISTER_CALLBACKS */
```

**参数**：
- `__HANDLE__`：SPI句柄，可以是SPI1、SPI2或SPI3

**返回值**：无

**作用**：重置SPI句柄状态

### __HAL_SPI_ENABLE_IT

**宏原型**：
```c
#define __HAL_SPI_ENABLE_IT(__HANDLE__, __INTERRUPT__)   SET_BIT((__HANDLE__)->Instance->CR2, (__INTERRUPT__))
```

**参数**：
- `__HANDLE__`：SPI句柄
- `__INTERRUPT__`：要启用的中断源，可以是：
  - `SPI_IT_TXE`：Tx缓冲区空中断使能
  - `SPI_IT_RXNE`：Rx缓冲区非空中断使能
  - `SPI_IT_ERR`：错误中断使能

**返回值**：无

**作用**：启用指定的SPI中断

### __HAL_SPI_DISABLE_IT

**宏原型**：
```c
#define __HAL_SPI_DISABLE_IT(__HANDLE__, __INTERRUPT__)  CLEAR_BIT((__HANDLE__)->Instance->CR2, (__INTERRUPT__))
```

**参数**：
- `__HANDLE__`：SPI句柄
- `__INTERRUPT__`：要禁用的中断源

**返回值**：无

**作用**：禁用指定的SPI中断

### __HAL_SPI_GET_IT_SOURCE

**宏原型**：
```c
#define __HAL_SPI_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) ((((__HANDLE__)->Instance->CR2 & (__INTERRUPT__)) == (__INTERRUPT__)) ? SET : RESET)
```

**参数**：
- `__HANDLE__`：SPI句柄
- `__INTERRUPT__`：要检查的SPI中断源

**返回值**：
- `SET`：中断源已启用
- `RESET`：中断源未启用

**作用**：检查指定的SPI中断源是否已启用

### __HAL_SPI_GET_FLAG

**宏原型**：
```c
#define __HAL_SPI_GET_FLAG(__HANDLE__, __FLAG__) ((((__HANDLE__)->Instance->SR) & (__FLAG__)) == (__FLAG__))
```

**参数**：
- `__HANDLE__`：SPI句柄
- `__FLAG__`：要检查的标志，可以是：
  - `SPI_FLAG_RXNE`：接收缓冲区非空标志
  - `SPI_FLAG_TXE`：发送缓冲区空标志
  - `SPI_FLAG_CRCERR`：CRC错误标志
  - `SPI_FLAG_MODF`：模式故障标志
  - `SPI_FLAG_OVR`：溢出标志
  - `SPI_FLAG_BSY`：忙标志

**返回值**：
- `TRUE`：标志已设置
- `FALSE`：标志未设置

**作用**：检查指定的SPI标志是否已设置

### __HAL_SPI_CLEAR_CRCERRFLAG

**宏原型**：
```c
#define __HAL_SPI_CLEAR_CRCERRFLAG(__HANDLE__) ((__HANDLE__)->Instance->SR = (uint16_t)(~SPI_FLAG_CRCERR))
```

**参数**：
- `__HANDLE__`：SPI句柄

**返回值**：无

**作用**：清除SPI CRCERR挂起标志

### __HAL_SPI_CLEAR_MODFFLAG

**宏原型**：
```c
#define __HAL_SPI_CLEAR_MODFFLAG(__HANDLE__)             \
  do{                                                    \
    __IO uint32_t tmpreg_modf = 0x00U;                   \
    tmpreg_modf = (__HANDLE__)->Instance->SR;            \
    CLEAR_BIT((__HANDLE__)->Instance->CR1, SPI_CR1_SPE); \
    UNUSED(tmpreg_modf);                                 \
  } while(0U)
```

**参数**：
- `__HANDLE__`：SPI句柄

**返回值**：无

**作用**：清除SPI MODF挂起标志

### __HAL_SPI_CLEAR_OVRFLAG

**宏原型**：
```c
#define __HAL_SPI_CLEAR_OVRFLAG(__HANDLE__)        \
  do{                                              \
    __IO uint32_t tmpreg_ovr = 0x00U;              \
    tmpreg_ovr = (__HANDLE__)->Instance->DR;       \
    tmpreg_ovr = (__HANDLE__)->Instance->SR;       \
    UNUSED(tmpreg_ovr);                            \
  } while(0U)
```

**参数**：
- `__HANDLE__`：SPI句柄

**返回值**：无

**作用**：清除SPI OVR挂起标志

### __HAL_SPI_ENABLE

**宏原型**：
```c
#define __HAL_SPI_ENABLE(__HANDLE__)  SET_BIT((__HANDLE__)->Instance->CR1, SPI_CR1_SPE)
```

**参数**：
- `__HANDLE__`：SPI句柄

**返回值**：无

**作用**：启用SPI外设

### __HAL_SPI_DISABLE

**宏原型**：
```c
#define __HAL_SPI_DISABLE(__HANDLE__) CLEAR_BIT((__HANDLE__)->Instance->CR1, SPI_CR1_SPE)
```

**参数**：
- `__HANDLE__`：SPI句柄

**返回值**：无

**作用**：禁用SPI外设

## 2. SPI 初始化函数

### HAL_SPI_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Init(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：指向SPI_HandleTypeDef结构体的指针，包含SPI模块的配置信息

**返回值**：
- `HAL_OK`：初始化成功
- `HAL_ERROR`：初始化失败
- `HAL_BUSY`：SPI忙
- `HAL_TIMEOUT`：超时

**作用**：根据指定参数初始化SPI

### HAL_SPI_DeInit

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_DeInit(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：指向SPI_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：去初始化成功
- `HAL_ERROR`：去初始化失败

**作用**：将SPI外设恢复到默认配置状态

### HAL_SPI_MspInit

**函数原型**：

```c
void HAL_SPI_MspInit(SPI_HandleTypeDef *hspi);
```

**参数**：

- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI MSP初始化函数，用于配置底层硬件资源

### HAL_SPI_MspDeInit

**函数原型**：

```c
void HAL_SPI_MspDeInit(SPI_HandleTypeDef *hspi);
```

**参数**：

- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI MSP去初始化函数，用于释放底层硬件资源

## 3. SPI 收发函数

### HAL_SPI_Transmit

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Transmit(SPI_HandleTypeDef *hspi, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `hspi`：SPI句柄
- `pData`：指向要发送数据的指针
- `Size`：要发送的数据大小（字节数）
- `Timeout`：超时时间（毫秒）

**返回值**：
- `HAL_OK`：发送成功
- `HAL_ERROR`：发送失败
- `HAL_BUSY`：SPI忙
- `HAL_TIMEOUT`：超时

**作用**：以轮询模式发送数据

### HAL_SPI_Receive

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Receive(SPI_HandleTypeDef *hspi, uint8_t *pData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `hspi`：SPI句柄
- `pData`：指向接收数据缓冲区的指针
- `Size`：要接收的数据大小（字节数）
- `Timeout`：超时时间（毫秒）

**返回值**：
- `HAL_OK`：接收成功
- `HAL_ERROR`：接收失败
- `HAL_BUSY`：SPI忙
- `HAL_TIMEOUT`：超时

**作用**：以轮询模式接收数据

### HAL_SPI_TransmitReceive

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_TransmitReceive(SPI_HandleTypeDef *hspi, uint8_t *pTxData, uint8_t *pRxData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `hspi`：SPI句柄
- `pTxData`：指向发送数据缓冲区的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要发送/接收的数据大小（字节数）
- `Timeout`：超时时间（毫秒）

**返回值**：
- `HAL_OK`：传输成功
- `HAL_ERROR`：传输失败
- `HAL_BUSY`：SPI忙
- `HAL_TIMEOUT`：超时

**作用**：以轮询模式同时发送和接收数据

### HAL_SPI_Transmit_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Transmit_IT(SPI_HandleTypeDef *hspi, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hspi`：SPI句柄
- `pData`：指向要发送数据的指针
- `Size`：要发送的数据大小（字节数）

**返回值**：
- `HAL_OK`：启动成功
- `HAL_ERROR`：启动失败
- `HAL_BUSY`：SPI忙

**作用**：以中断模式发送数据

### HAL_SPI_Receive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Receive_IT(SPI_HandleTypeDef *hspi, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hspi`：SPI句柄
- `pData`：指向接收数据缓冲区的指针
- `Size`：要接收的数据大小（字节数）

**返回值**：
- `HAL_OK`：启动成功
- `HAL_ERROR`：启动失败
- `HAL_BUSY`：SPI忙

**作用**：以中断模式接收数据

### HAL_SPI_TransmitReceive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_TransmitReceive_IT(SPI_HandleTypeDef *hspi, uint8_t *pTxData, uint8_t *pRxData, uint16_t Size);
```

**参数**：
- `hspi`：SPI句柄
- `pTxData`：指向发送数据缓冲区的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要发送/接收的数据大小（字节数）

**返回值**：
- `HAL_OK`：启动成功
- `HAL_ERROR`：启动失败
- `HAL_BUSY`：SPI忙

**作用**：以中断模式同时发送和接收数据

### HAL_SPI_Transmit_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Transmit_DMA(SPI_HandleTypeDef *hspi, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hspi`：SPI句柄
- `pData`：指向要发送数据的指针
- `Size`：要发送的数据大小（字节数）

**返回值**：
- `HAL_OK`：启动成功
- `HAL_ERROR`：启动失败
- `HAL_BUSY`：SPI忙

**作用**：以DMA模式发送数据

### HAL_SPI_Receive_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Receive_DMA(SPI_HandleTypeDef *hspi, uint8_t *pData, uint16_t Size);
```

**参数**：
- `hspi`：SPI句柄
- `pData`：指向接收数据缓冲区的指针
- `Size`：要接收的数据大小（字节数）

**返回值**：
- `HAL_OK`：启动成功
- `HAL_ERROR`：启动失败
- `HAL_BUSY`：SPI忙

**作用**：以DMA模式接收数据

### HAL_SPI_TransmitReceive_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_TransmitReceive_DMA(SPI_HandleTypeDef *hspi, uint8_t *pTxData, uint8_t *pRxData, uint16_t Size);
```

**参数**：
- `hspi`：SPI句柄
- `pTxData`：指向发送数据缓冲区的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要发送/接收的数据大小（字节数）

**返回值**：
- `HAL_OK`：启动成功
- `HAL_ERROR`：启动失败
- `HAL_BUSY`：SPI忙

**作用**：以DMA模式同时发送和接收数据

## 4. SPI 控制函数

### HAL_SPI_Abort

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Abort(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_OK`：中止成功
- `HAL_ERROR`：中止失败
- `HAL_BUSY`：SPI忙

**作用**：中止SPI传输

### HAL_SPI_Abort_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_Abort_IT(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_OK`：中止成功
- `HAL_ERROR`：中止失败
- `HAL_BUSY`：SPI忙

**作用**：以中断模式中止SPI传输

### HAL_SPI_DMAPause

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_DMAPause(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_OK`：暂停成功
- `HAL_ERROR`：暂停失败

**作用**：暂停SPI DMA传输

### HAL_SPI_DMAResume

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_DMAResume(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_OK`：恢复成功
- `HAL_ERROR`：恢复失败

**作用**：恢复SPI DMA传输

### HAL_SPI_DMAStop

**函数原型**：
```c
HAL_StatusTypeDef HAL_SPI_DMAStop(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_OK`：停止成功
- `HAL_ERROR`：停止失败

**作用**：停止SPI DMA传输

## 5. SPI 状态函数

### HAL_SPI_GetState

**函数原型**：
```c
HAL_SPI_StateTypeDef HAL_SPI_GetState(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_SPI_STATE_RESET`：外设未初始化
- `HAL_SPI_STATE_READY`：外设已初始化并准备就绪
- `HAL_SPI_STATE_BUSY`：内部进程正在进行
- `HAL_SPI_STATE_BUSY_TX`：数据发送进程正在进行
- `HAL_SPI_STATE_BUSY_RX`：数据接收进程正在进行
- `HAL_SPI_STATE_BUSY_TX_RX`：数据发送和接收进程正在进行
- `HAL_SPI_STATE_ERROR`：SPI错误状态
- `HAL_SPI_STATE_ABORT`：SPI中止正在进行

**作用**：获取SPI当前状态

### HAL_SPI_GetError

**函数原型**：
```c
uint32_t HAL_SPI_GetError(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：
- `HAL_SPI_ERROR_NONE`：无错误
- `HAL_SPI_ERROR_MODF`：MODF错误
- `HAL_SPI_ERROR_CRC`：CRC错误
- `HAL_SPI_ERROR_OVR`：OVR错误
- `HAL_SPI_ERROR_DMA`：DMA传输错误
- `HAL_SPI_ERROR_FLAG`：RXNE/TXE/BSY标志错误
- `HAL_SPI_ERROR_ABORT`：中止过程错误
- `HAL_SPI_ERROR_INVALID_CALLBACK`：无效回调错误

**作用**：获取SPI错误代码

## 6. SPI 中断处理函数

### HAL_SPI_IRQHandler

**函数原型**：

```c
void HAL_SPI_IRQHandler(SPI_HandleTypeDef *hspi);
```

**参数**：

- `hspi`：SPI句柄

**返回值**：无

**作用**：处理SPI中断请求，管理SPI接收、发送和错误中断

## 7. SPI 回调函数

### HAL_SPI_TxCpltCallback

**函数原型**：
```c
void HAL_SPI_TxCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI发送完成回调函数

### HAL_SPI_RxCpltCallback

**函数原型**：
```c
void HAL_SPI_RxCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI接收完成回调函数

### HAL_SPI_TxRxCpltCallback

**函数原型**：
```c
void HAL_SPI_TxRxCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI发送接收完成回调函数

### HAL_SPI_TxHalfCpltCallback

**函数原型**：
```c
void HAL_SPI_TxHalfCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI发送半完成回调函数

### HAL_SPI_RxHalfCpltCallback

**函数原型**：
```c
void HAL_SPI_RxHalfCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI接收半完成回调函数

### HAL_SPI_TxRxHalfCpltCallback

**函数原型**：
```c
void HAL_SPI_TxRxHalfCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI发送接收半完成回调函数

### HAL_SPI_AbortCpltCallback

**函数原型**：

```c
void HAL_SPI_AbortCpltCallback(SPI_HandleTypeDef *hspi);
```

**参数**：

- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI中止完成回调函数

### HAL_SPI_ErrorCallback

**函数原型**：
```c
void HAL_SPI_ErrorCallback(SPI_HandleTypeDef *hspi);
```

**参数**：
- `hspi`：SPI句柄

**返回值**：无

**作用**：SPI错误回调函数

### HAL_SPI_RegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_SPI_RegisterCallback(SPI_HandleTypeDef *hspi,
                                           HAL_SPI_CallbackIDTypeDef CallbackID,
                                           pSPI_CallbackTypeDef pCallback);
```

**参数**：

- `hspi`：SPI句柄
- `CallbackID`：回调函数ID，可以是 `HAL_SPI_CallbackIDTypeDef` 枚举中的值
- `pCallback`：用户定义的回调函数指针

**返回值**：

- `HAL_OK`：注册成功
- `HAL_ERROR`：注册失败

**作用**：注册SPI回调函数（需在 `USE_HAL_SPI_REGISTER_CALLBACKS = 1` 时可用）

### HAL_SPI_UnRegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_SPI_UnRegisterCallback(SPI_HandleTypeDef *hspi,
                                           HAL_SPI_CallbackIDTypeDef CallbackID);
```

**参数**：

- `hspi`：SPI句柄
- `CallbackID`：回调函数ID（同上）

**返回值**：

- `HAL_OK`：注销成功
- `HAL_ERROR`：注销失败

**作用**：注销SPI回调函数，恢复为默认的弱定义回调