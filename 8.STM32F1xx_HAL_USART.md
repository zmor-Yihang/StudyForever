# STM32F1xx HAL USART库函数说明文档

本文档介绍了STM32F1xx HAL库中的USART函数，包括函数原型、参数、返回值和具体作用。

## 1. USART __HAL 宏函数

### __HAL_USART_ENABLE

**宏原型**：

```c
#define __HAL_USART_ENABLE(__HANDLE__) ((__HANDLE__)->Instance->CR1 |= USART_CR1_UE)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：启用USART外设

### __HAL_USART_DISABLE

**宏原型**：

```c
#define __HAL_USART_DISABLE(__HANDLE__) ((__HANDLE__)->Instance->CR1 &= ~USART_CR1_UE)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：禁用USART外设

### __HAL_USART_GET_FLAG

**宏原型**：

```c
#define __HAL_USART_GET_FLAG(__HANDLE__, __FLAG__) (((__HANDLE__)->Instance->SR & (__FLAG__)) == (__FLAG__))
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体
- `__FLAG__`：要检查的标志位

**返回值**：

- 返回标志状态（SET或RESET）

**作用**：检查指定的USART标志位是否被置位

### __HAL_USART_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_USART_CLEAR_FLAG(__HANDLE__, __FLAG__) ((__HANDLE__)->Instance->SR &= ~(__FLAG__))
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体
- `__FLAG__`：要清除的标志位

**返回值**：无

**作用**：清除USART的指定标志位

### __HAL_USART_CLEAR_PEFLAG

**宏原型**：

```c
#define __HAL_USART_CLEAR_PEFLAG(__HANDLE__)    \
  do{                                           \
    __IO uint32_t tmpreg = 0x00U;               \
    tmpreg = (__HANDLE__)->Instance->SR;        \
    tmpreg = (__HANDLE__)->Instance->DR;        \
    UNUSED(tmpreg);                             \
  } while(0U)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：清除USART的奇偶校验错误标志位

### __HAL_USART_CLEAR_FEFLAG

**宏原型**：

```c
#define __HAL_USART_CLEAR_FEFLAG(__HANDLE__) __HAL_USART_CLEAR_PEFLAG(__HANDLE__)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：清除USART的帧错误标志位

### __HAL_USART_CLEAR_NEFLAG

**宏原型**：

```c
#define __HAL_USART_CLEAR_NEFLAG(__HANDLE__) __HAL_USART_CLEAR_PEFLAG(__HANDLE__)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：清除USART的噪声错误标志位

### __HAL_USART_CLEAR_OREFLAG

**宏原型**：

```c
#define __HAL_USART_CLEAR_OREFLAG(__HANDLE__) __HAL_USART_CLEAR_PEFLAG(__HANDLE__)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：清除USART的溢出错误标志位

### __HAL_USART_CLEAR_IDLEFLAG

**宏原型**：

```c
#define __HAL_USART_CLEAR_IDLEFLAG(__HANDLE__) __HAL_USART_CLEAR_PEFLAG(__HANDLE__)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：清除USART的空闲检测标志位

### __HAL_USART_ENABLE_IT

**宏原型**：

```c
#define __HAL_USART_ENABLE_IT(__HANDLE__, __INTERRUPT__) ((((__INTERRUPT__) >> 28U) == USART_CR1_REG_INDEX)? ((__HANDLE__)->Instance->CR1 |= ((__INTERRUPT__) & USART_IT_MASK)): \
                                                            (((__INTERRUPT__) >> 28U) == USART_CR2_REG_INDEX)? ((__HANDLE__)->Instance->CR2 |= ((__INTERRUPT__) & USART_IT_MASK)): \
                                                             ((__HANDLE__)->Instance->CR3 |= ((__INTERRUPT__) & USART_IT_MASK)))
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体
- `__INTERRUPT__`：要启用的中断

**返回值**：无

**作用**：启用指定的USART中断

### __HAL_USART_DISABLE_IT

**宏原型**：

```c
#define __HAL_USART_DISABLE_IT(__HANDLE__, __INTERRUPT__)  ((((__INTERRUPT__) >> 28U) == USART_CR1_REG_INDEX)? ((__HANDLE__)->Instance->CR1 &= ~((__INTERRUPT__) & USART_IT_MASK)): \
                                                            (((__INTERRUPT__) >> 28U) == USART_CR2_REG_INDEX)? ((__HANDLE__)->Instance->CR2 &= ~((__INTERRUPT__) & USART_IT_MASK)): \
                                                             ((__HANDLE__)->Instance->CR3 &= ~ ((__INTERRUPT__) & USART_IT_MASK)))
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体
- `__INTERRUPT__`：要禁用的中断

**返回值**：无

**作用**：禁用指定的USART中断

### __HAL_USART_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_USART_GET_IT_SOURCE(__HANDLE__, __IT__) (((((__IT__) >> 28U) == USART_CR1_REG_INDEX)? (__HANDLE__)->Instance->CR1:(((((uint32_t)(__IT__)) >> 28U) == USART_CR2_REG_INDEX)? \
                                                      (__HANDLE__)->Instance->CR2 : (__HANDLE__)->Instance->CR3)) & (((uint32_t)(__IT__)) & USART_IT_MASK))
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体
- `__IT__`：要检查的中断源

**返回值**：

- 返回中断源状态（SET或RESET）

**作用**：检查指定的USART中断源是否被启用

### __HAL_USART_ONE_BIT_SAMPLE_ENABLE

**宏原型**：

```c
#define __HAL_USART_ONE_BIT_SAMPLE_ENABLE(__HANDLE__) ((__HANDLE__)->Instance->CR3 |= USART_CR3_ONEBIT)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：启用USART的一位采样方法

### __HAL_USART_ONE_BIT_SAMPLE_DISABLE

**宏原型**：

```c
#define __HAL_USART_ONE_BIT_SAMPLE_DISABLE(__HANDLE__) ((__HANDLE__)->Instance->CR3 &= (uint16_t)~((uint16_t)USART_CR3_ONEBIT))
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：禁用USART的一位采样方法

### __HAL_USART_RESET_HANDLE_STATE

**宏原型**：

```c
#define __HAL_USART_RESET_HANDLE_STATE(__HANDLE__)  ((__HANDLE__)->State = HAL_USART_STATE_RESET)
```

**参数**：

- `__HANDLE__`：USART句柄，指向一个USART_HandleTypeDef结构体

**返回值**：无

**作用**：重置USART句柄状态

## 2. USART 初始化与反初始化函数

### HAL_USART_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Init(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针，包含USART配置信息

**返回值**：
- `HAL_OK`：初始化成功
- `HAL_ERROR`：初始化失败

**作用**：根据USART_HandleTypeDef中的参数初始化USART外设

### HAL_USART_DeInit

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_DeInit(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：反初始化成功
- `HAL_ERROR`：反初始化失败

**作用**：将USART外设反初始化为默认配置

### HAL_USART_MspInit

**函数原型**：
```c
void HAL_USART_MspInit(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：初始化USART的MSP (MCU Specific Package)，包括GPIO配置、时钟配置等底层初始化

### HAL_USART_MspDeInit

**函数原型**：
```c
void HAL_USART_MspDeInit(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：反初始化USART的MSP (MCU Specific Package)，包括GPIO反初始化等

## 3. USART 数据传输函数

### HAL_USART_Transmit

**函数原型**：

```c
HAL_StatusTypeDef HAL_USART_Transmit(USART_HandleTypeDef *husart, uint8_t *pTxData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pTxData`：指向待发送数据缓冲区的指针
- `Size`：要发送的数据元素数量
- `Timeout`：超时时间，单位为毫秒

**返回值**：
- `HAL_OK`：传输成功
- `HAL_ERROR`：传输过程中出错
- `HAL_BUSY`：USART处于忙状态
- `HAL_TIMEOUT`：传输超时

**作用**：以阻塞模式发送指定数量的数据

### HAL_USART_Receive

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Receive(USART_HandleTypeDef *husart, uint8_t *pRxData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要接收的数据元素数量
- `Timeout`：超时时间，单位为毫秒

**返回值**：
- `HAL_OK`：接收成功
- `HAL_ERROR`：接收过程中出错
- `HAL_BUSY`：USART处于忙状态
- `HAL_TIMEOUT`：接收超时

**作用**：以阻塞模式接收指定数量的数据

### HAL_USART_TransmitReceive

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_TransmitReceive(USART_HandleTypeDef *husart, uint8_t *pTxData, uint8_t *pRxData, uint16_t Size, uint32_t Timeout);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pTxData`：指向发送数据缓冲区的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要发送和接收的数据元素数量
- `Timeout`：超时时间，单位为毫秒

**返回值**：
- `HAL_OK`：传输接收成功
- `HAL_ERROR`：过程中出错
- `HAL_BUSY`：USART处于忙状态
- `HAL_TIMEOUT`：操作超时

**作用**：以阻塞模式同时发送和接收指定数量的数据

### HAL_USART_Transmit_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Transmit_IT(USART_HandleTypeDef *husart, uint8_t *pTxData, uint16_t Size);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pTxData`：指向待发送数据缓冲区的指针
- `Size`：要发送的数据元素数量

**返回值**：
- `HAL_OK`：传输成功启动
- `HAL_ERROR`：传输启动失败
- `HAL_BUSY`：USART处于忙状态

**作用**：以中断模式发送指定数量的数据

### HAL_USART_Receive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Receive_IT(USART_HandleTypeDef *husart, uint8_t *pRxData, uint16_t Size);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要接收的数据元素数量

**返回值**：
- `HAL_OK`：接收成功启动
- `HAL_ERROR`：接收启动失败
- `HAL_BUSY`：USART处于忙状态

**作用**：以中断模式接收指定数量的数据

### HAL_USART_TransmitReceive_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_TransmitReceive_IT(USART_HandleTypeDef *husart, uint8_t *pTxData, uint8_t *pRxData, uint16_t Size);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pTxData`：指向发送数据缓冲区的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要发送和接收的数据元素数量

**返回值**：
- `HAL_OK`：传输接收成功启动
- `HAL_ERROR`：过程启动失败
- `HAL_BUSY`：USART处于忙状态

**作用**：以中断模式同时发送和接收指定数量的数据

### HAL_USART_Transmit_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Transmit_DMA(USART_HandleTypeDef *husart, uint8_t *pTxData, uint16_t Size);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pTxData`：指向待发送数据缓冲区的指针
- `Size`：要发送的数据元素数量

**返回值**：
- `HAL_OK`：传输成功启动
- `HAL_ERROR`：传输启动失败
- `HAL_BUSY`：USART处于忙状态

**作用**：以DMA模式发送指定数量的数据

### HAL_USART_Receive_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Receive_DMA(USART_HandleTypeDef *husart, uint8_t *pRxData, uint16_t Size);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要接收的数据元素数量

**返回值**：
- `HAL_OK`：接收成功启动
- `HAL_ERROR`：接收启动失败
- `HAL_BUSY`：USART处于忙状态

**作用**：以DMA模式接收指定数量的数据

### HAL_USART_TransmitReceive_DMA

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_TransmitReceive_DMA(USART_HandleTypeDef *husart, uint8_t *pTxData, uint8_t *pRxData, uint16_t Size);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `pTxData`：指向发送数据缓冲区的指针
- `pRxData`：指向接收数据缓冲区的指针
- `Size`：要发送和接收的数据元素数量

**返回值**：
- `HAL_OK`：传输接收成功启动
- `HAL_ERROR`：过程启动失败
- `HAL_BUSY`：USART处于忙状态

**作用**：以DMA模式同时发送和接收指定数量的数据

## 4.USART DMA控制函数

### HAL_USART_DMAPause

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_DMAPause(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：DMA传输暂停成功

**作用**：暂停USART的DMA传输

### HAL_USART_DMAResume

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_DMAResume(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：DMA传输恢复成功

**作用**：恢复USART的DMA传输

### HAL_USART_DMAStop

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_DMAStop(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：DMA传输停止成功

**作用**：停止USART的DMA传输

## 4.USART中止传输函数

### HAL_USART_Abort

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Abort(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：终止传输成功

**作用**：终止当前的USART传输(阻塞模式)

### HAL_USART_Abort_IT

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_Abort_IT(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- `HAL_OK`：终止传输成功启动

**作用**：终止当前的USART传输(非阻塞模式)

## 4. USART 中断处理

### HAL_USART_IRQHandler

**函数原型**：
```c
void HAL_USART_IRQHandler(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理USART中断请求



## 5. USART 回调函数

### HAL_USART_TxCpltCallback

**函数原型**：

```c
void HAL_USART_TxCpltCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：发送完成回调函数，在HAL_USART_IRQHandler中被调用

### HAL_USART_TxHalfCpltCallback

**函数原型**：

```c
void HAL_USART_TxHalfCpltCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：发送一半完成回调函数，在HAL_USART_IRQHandler中被调用

### HAL_USART_RxCpltCallback

**函数原型**：

```c
void HAL_USART_RxCpltCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：接收完成回调函数，在HAL_USART_IRQHandler中被调用

### HAL_USART_RxHalfCpltCallback

**函数原型**：

```c
void HAL_USART_RxHalfCpltCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：接收一半完成回调函数，在HAL_USART_IRQHandler中被调用

### HAL_USART_TxRxCpltCallback

**函数原型**：

```c
void HAL_USART_TxRxCpltCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：发送接收完成回调函数，在HAL_USART_IRQHandler中被调用

### HAL_USART_ErrorCallback

**函数原型**：

```c
void HAL_USART_ErrorCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：USART错误回调函数，在HAL_USART_IRQHandler中被调用

### HAL_USART_AbortCpltCallback

**函数原型**：

```c
void HAL_USART_AbortCpltCallback(USART_HandleTypeDef *husart);
```

**参数**：

- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：无

**作用**：终止操作完成回调函数

### HAL_USART_RegisterCallback

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_RegisterCallback(USART_HandleTypeDef *husart, HAL_USART_CallbackIDTypeDef CallbackID, pUSART_CallbackTypeDef pCallback);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `CallbackID`：回调ID，表明要注册的回调类型
- `pCallback`：指向回调函数的指针

**返回值**：
- `HAL_OK`：注册成功
- `HAL_ERROR`：注册失败

**作用**：注册用户USART回调函数

### HAL_USART_UnRegisterCallback

**函数原型**：
```c
HAL_StatusTypeDef HAL_USART_UnRegisterCallback(USART_HandleTypeDef *husart, HAL_USART_CallbackIDTypeDef CallbackID);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针
- `CallbackID`：回调ID，表明要取消注册的回调类型

**返回值**：
- `HAL_OK`：取消注册成功
- `HAL_ERROR`：取消注册失败

**作用**：取消注册用户USART回调函数

## 6. USART 状态和错误处理函数

### HAL_USART_GetState

**函数原型**：
```c
HAL_USART_StateTypeDef HAL_USART_GetState(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- 返回USART的当前状态

**作用**：获取USART的当前状态

### HAL_USART_GetError

**函数原型**：
```c
uint32_t HAL_USART_GetError(USART_HandleTypeDef *husart);
```

**参数**：
- `husart`：指向USART_HandleTypeDef结构体的指针

**返回值**：
- 返回USART的错误代码

**作用**：获取USART的错误代码
