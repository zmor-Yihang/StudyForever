# STM32F1xx HAL RTC库函数说明文档

本文档介绍了STM32F1xx HAL库中的RTC（实时时钟）函数，包括函数原型、参数、返回值和具体作用。

## 1. __HAL 宏函数

### __HAL_RTC_RESET_HANDLE_STATE

**宏原型**：

```c
#define __HAL_RTC_RESET_HANDLE_STATE(__HANDLE__) ((__HANDLE__)->State = HAL_RTC_STATE_RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄

**返回值**：无

**作用**：重置RTC句柄状态

### __HAL_RTC_WRITEPROTECTION_DISABLE

**宏原型**：

```c
#define __HAL_RTC_WRITEPROTECTION_DISABLE(__HANDLE__) SET_BIT((__HANDLE__)->Instance->CRL, RTC_CRL_CNF)
```

**参数**：

- `__HANDLE__`：RTC句柄

**返回值**：无

**作用**：禁用RTC寄存器的写保护

### __HAL_RTC_WRITEPROTECTION_ENABLE

**宏原型**：

```c
#define __HAL_RTC_WRITEPROTECTION_ENABLE(__HANDLE__) CLEAR_BIT((__HANDLE__)->Instance->CRL, RTC_CRL_CNF)
```

**参数**：

- `__HANDLE__`：RTC句柄

**返回值**：无

**作用**：启用RTC寄存器的写保护

### __HAL_RTC_ALARM_ENABLE_IT

**宏原型**：

```c
#define __HAL_RTC_ALARM_ENABLE_IT(__HANDLE__, __INTERRUPT__) SET_BIT((__HANDLE__)->Instance->CRH, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要启用的RTC报警中断源，可以是RTC_IT_ALRA

**返回值**：无

**作用**：启用RTC报警中断

### __HAL_RTC_ALARM_DISABLE_IT

**宏原型**：

```c
#define __HAL_RTC_ALARM_DISABLE_IT(__HANDLE__, __INTERRUPT__) CLEAR_BIT((__HANDLE__)->Instance->CRH, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要禁用的RTC报警中断源，可以是RTC_IT_ALRA

**返回值**：无

**作用**：禁用RTC报警中断

### __HAL_RTC_ALARM_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_RTC_ALARM_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) ((((((__HANDLE__)->Instance->CRH)& ((__INTERRUPT__)))) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要检查的RTC报警中断源，可以是RTC_IT_ALRA

**返回值**：

- `SET`：如果中断源已启用
- `RESET`：如果中断源未启用

**作用**：检查指定的RTC报警中断源是否已启用

### __HAL_RTC_ALARM_GET_FLAG

**宏原型**：

```c
#define __HAL_RTC_ALARM_GET_FLAG(__HANDLE__, __FLAG__) (((((__HANDLE__)->Instance->CRL) & (__FLAG__)) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要检查的RTC报警标志，可以是RTC_FLAG_ALRAF

**返回值**：

- `SET`：如果标志被置位
- `RESET`：如果标志未被置位

**作用**：获取选定的RTC报警标志状态

### __HAL_RTC_ALARM_GET_IT

**宏原型**：

```c
#define __HAL_RTC_ALARM_GET_IT(__HANDLE__, __INTERRUPT__) (((((__HANDLE__)->Instance->CRL) & (__INTERRUPT__)) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要检查的RTC报警中断，可以是RTC_IT_ALRA

**返回值**：

- `SET`：如果中断已发生
- `RESET`：如果中断未发生

**作用**：检查指定的RTC报警中断是否已发生

### __HAL_RTC_ALARM_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_RTC_ALARM_CLEAR_FLAG(__HANDLE__, __FLAG__) ((__HANDLE__)->Instance->CRL) = ~(__FLAG__)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要清除的RTC报警标志，可以是RTC_FLAG_ALRAF

**返回值**：无

**作用**：清除RTC报警的挂起标志

### __HAL_RTC_ALARM_EXTI_ENABLE_IT

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_ENABLE_IT() SET_BIT(EXTI->IMR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：在EXTI线17上启用RTC报警中断

### __HAL_RTC_ALARM_EXTI_DISABLE_IT

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_DISABLE_IT() CLEAR_BIT(EXTI->IMR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：在EXTI线17上禁用RTC报警中断

### __HAL_RTC_ALARM_EXTI_ENABLE_EVENT

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_ENABLE_EVENT() SET_BIT(EXTI->EMR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：在EXTI线17上启用RTC报警事件

### __HAL_RTC_ALARM_EXTI_DISABLE_EVENT

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_DISABLE_EVENT() CLEAR_BIT(EXTI->EMR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：在EXTI线17上禁用RTC报警事件

### __HAL_RTC_ALARM_EXTI_ENABLE_FALLING_EDGE

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_ENABLE_FALLING_EDGE() SET_BIT(EXTI->FTSR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：配置RTC报警EXTI线为下降沿触发

### __HAL_RTC_ALARM_EXTI_DISABLE_FALLING_EDGE

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_DISABLE_FALLING_EDGE() CLEAR_BIT(EXTI->FTSR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：禁用RTC报警扩展中断下降沿触发

### __HAL_RTC_ALARM_EXTI_ENABLE_RISING_EDGE

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_ENABLE_RISING_EDGE() SET_BIT(EXTI->RTSR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：配置RTC报警EXTI线为上升沿触发

### __HAL_RTC_ALARM_EXTI_DISABLE_RISING_EDGE

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_DISABLE_RISING_EDGE() CLEAR_BIT(EXTI->RTSR, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：禁用RTC报警扩展中断上升沿触发

### __HAL_RTC_ALARM_EXTI_ENABLE_RISING_FALLING_EDGE

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_ENABLE_RISING_FALLING_EDGE() \
do{ \
    __HAL_RTC_ALARM_EXTI_ENABLE_RISING_EDGE(); \
    __HAL_RTC_ALARM_EXTI_ENABLE_FALLING_EDGE(); \
} while(0U)
```

**参数**：无

**返回值**：无

**作用**：配置RTC报警EXTI线为上升沿和下降沿触发

### __HAL_RTC_ALARM_EXTI_DISABLE_RISING_FALLING_EDGE

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_DISABLE_RISING_FALLING_EDGE() \
do{ \
    __HAL_RTC_ALARM_EXTI_DISABLE_RISING_EDGE(); \
    __HAL_RTC_ALARM_EXTI_DISABLE_FALLING_EDGE(); \
} while(0U)
```

**参数**：无

**返回值**：无

**作用**：禁用RTC报警扩展中断上升沿和下降沿触发

### __HAL_RTC_ALARM_EXTI_GET_FLAG

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_GET_FLAG() (EXTI->PR & (RTC_EXTI_LINE_ALARM_EVENT))
```

**参数**：无

**返回值**：

- 返回EXTI报警线状态

**作用**：检查指定的RTC报警EXTI中断标志是否置位

### __HAL_RTC_ALARM_EXTI_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_CLEAR_FLAG() (EXTI->PR = (RTC_EXTI_LINE_ALARM_EVENT))
```

**参数**：无

**返回值**：无

**作用**：清除RTC报警EXTI标志

### __HAL_RTC_ALARM_EXTI_GENERATE_SWIT

**宏原型**：

```c
#define __HAL_RTC_ALARM_EXTI_GENERATE_SWIT() SET_BIT(EXTI->SWIER, RTC_EXTI_LINE_ALARM_EVENT)
```

**参数**：无

**返回值**：无

**作用**：在选定的EXTI线上生成软件中断

### __HAL_RTC_TAMPER_ENABLE_IT

**宏原型**：

```c
#define __HAL_RTC_TAMPER_ENABLE_IT(__HANDLE__, __INTERRUPT__) SET_BIT(BKP->CSR, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要启用的RTC防篡改中断源，可以是RTC_IT_TAMP1

**返回值**：无

**作用**：启用RTC防篡改中断

### __HAL_RTC_TAMPER_DISABLE_IT

**宏原型**：

```c
#define __HAL_RTC_TAMPER_DISABLE_IT(__HANDLE__, __INTERRUPT__) CLEAR_BIT(BKP->CSR, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要禁用的RTC防篡改中断源，可以是RTC_IT_TAMP1

**返回值**：无

**作用**：禁用RTC防篡改中断

### __HAL_RTC_TAMPER_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_RTC_TAMPER_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) ((((BKP->CSR) & ((__INTERRUPT__))) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要检查的RTC防篡改中断源，可以是RTC_IT_TAMP1

**返回值**：

- `SET`：如果中断源已启用
- `RESET`：如果中断源未启用

**作用**：检查指定的RTC防篡改中断源是否已启用

### __HAL_RTC_TAMPER_GET_FLAG

**宏原型**：

```c
#define __HAL_RTC_TAMPER_GET_FLAG(__HANDLE__, __FLAG__) ((((BKP->CSR) & (__FLAG__)) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要检查的RTC防篡改标志，可以是RTC_FLAG_TAMP1F

**返回值**：

- `SET`：如果标志被置位
- `RESET`：如果标志未被置位

**作用**：获取选定的RTC防篡改标志状态

### __HAL_RTC_TAMPER_GET_IT

**宏原型**：

```c
#define __HAL_RTC_TAMPER_GET_IT(__HANDLE__, __INTERRUPT__) ((((BKP->CSR) & (BKP_CSR_TEF)) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要检查的RTC防篡改中断，可以是RTC_IT_TAMP1

**返回值**：

- `SET`：如果中断已发生
- `RESET`：如果中断未发生

**作用**：检查指定的RTC防篡改中断是否已发生

### __HAL_RTC_TAMPER_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_RTC_TAMPER_CLEAR_FLAG(__HANDLE__, __FLAG__) SET_BIT(BKP->CSR, BKP_CSR_CTE | BKP_CSR_CTI)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要清除的RTC防篡改标志，可以是RTC_FLAG_TAMP1F

**返回值**：无

**作用**：清除RTC防篡改的挂起标志

### __HAL_RTC_SECOND_ENABLE_IT

**宏原型**：

```c
#define __HAL_RTC_SECOND_ENABLE_IT(__HANDLE__, __INTERRUPT__) SET_BIT((__HANDLE__)->Instance->CRH, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要启用的RTC秒中断源，可以是RTC_IT_SEC

**返回值**：无

**作用**：启用RTC秒中断

### __HAL_RTC_SECOND_DISABLE_IT

**宏原型**：

```c
#define __HAL_RTC_SECOND_DISABLE_IT(__HANDLE__, __INTERRUPT__) CLEAR_BIT((__HANDLE__)->Instance->CRH, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要禁用的RTC秒中断源，可以是RTC_IT_SEC

**返回值**：无

**作用**：禁用RTC秒中断

### __HAL_RTC_SECOND_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_RTC_SECOND_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) ((((((__HANDLE__)->Instance->CRH)& ((__INTERRUPT__)))) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要检查的RTC秒中断源，可以是RTC_IT_SEC

**返回值**：

- `SET`：如果中断源已启用
- `RESET`：如果中断源未启用

**作用**：检查指定的RTC秒中断源是否已启用

### __HAL_RTC_SECOND_GET_FLAG

**宏原型**：

```c
#define __HAL_RTC_SECOND_GET_FLAG(__HANDLE__, __FLAG__) (((((__HANDLE__)->Instance->CRL) & (__FLAG__)) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要检查的RTC秒标志，可以是RTC_FLAG_SEC

**返回值**：

- `SET`：如果标志被置位
- `RESET`：如果标志未被置位

**作用**：获取选定的RTC秒标志状态

### __HAL_RTC_SECOND_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_RTC_SECOND_CLEAR_FLAG(__HANDLE__, __FLAG__) ((__HANDLE__)->Instance->CRL) = ~(__FLAG__)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要清除的RTC秒标志，可以是RTC_FLAG_SEC

**返回值**：无

**作用**：清除RTC秒的挂起标志

### __HAL_RTC_OVERFLOW_ENABLE_IT

**宏原型**：

```c
#define __HAL_RTC_OVERFLOW_ENABLE_IT(__HANDLE__, __INTERRUPT__) SET_BIT((__HANDLE__)->Instance->CRH, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要启用的RTC溢出中断源，可以是RTC_IT_OW

**返回值**：无

**作用**：启用RTC溢出中断

### __HAL_RTC_OVERFLOW_DISABLE_IT

**宏原型**：

```c
#define __HAL_RTC_OVERFLOW_DISABLE_IT(__HANDLE__, __INTERRUPT__) CLEAR_BIT((__HANDLE__)->Instance->CRH, (__INTERRUPT__))
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要禁用的RTC溢出中断源，可以是RTC_IT_OW

**返回值**：无

**作用**：禁用RTC溢出中断

### __HAL_RTC_OVERFLOW_GET_IT_SOURCE

**宏原型**：

```c
#define __HAL_RTC_OVERFLOW_GET_IT_SOURCE(__HANDLE__, __INTERRUPT__) ((((((__HANDLE__)->Instance->CRH)& ((__INTERRUPT__))) ) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__INTERRUPT__`：要检查的RTC溢出中断源，可以是RTC_IT_OW

**返回值**：

- `SET`：如果中断源已启用
- `RESET`：如果中断源未启用

**作用**：检查指定的RTC溢出中断源是否已启用

### __HAL_RTC_OVERFLOW_GET_FLAG

**宏原型**：

```c
#define __HAL_RTC_OVERFLOW_GET_FLAG(__HANDLE__, __FLAG__) (((((__HANDLE__)->Instance->CRL) & (__FLAG__)) != RESET)? SET : RESET)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要检查的RTC溢出标志，可以是RTC_FLAG_OW

**返回值**：

- `SET`：如果标志被置位
- `RESET`：如果标志未被置位

**作用**：获取选定的RTC溢出标志状态

### __HAL_RTC_OVERFLOW_CLEAR_FLAG

**宏原型**：

```c
#define __HAL_RTC_OVERFLOW_CLEAR_FLAG(__HANDLE__, __FLAG__) ((__HANDLE__)->Instance->CRL) = ~(__FLAG__)
```

**参数**：

- `__HANDLE__`：RTC句柄
- `__FLAG__`：要清除的RTC溢出标志，可以是RTC_FLAG_OW

**返回值**：无

**作用**：清除RTC溢出的挂起标志

## 2. RTC 初始化函数

### HAL_RTC_Init

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_Init(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针，包含RTC配置信息

**返回值**：

- `HAL_OK`：初始化成功
- `HAL_ERROR`：初始化失败

**作用**：根据hrtc中指定的参数初始化RTC外设

### HAL_RTC_DeInit

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_DeInit(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：反初始化成功
- `HAL_ERROR`：反初始化失败

**作用**：将RTC外设重置为默认状态

### HAL_RTC_MspInit

**函数原型**：

```c
void HAL_RTC_MspInit(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：RTC MSP初始化回调函数，用户可重新定义此函数

### HAL_RTC_MspDeInit

**函数原型**：

```c
void HAL_RTC_MspDeInit(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：RTC MSP反初始化回调函数，用户可重新定义此函数

## 3. RTC 操作函数

### HAL_RTC_SetTime

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_SetTime(RTC_HandleTypeDef *hrtc, RTC_TimeTypeDef *sTime, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sTime`：指向RTC_TimeTypeDef结构体的指针，包含时间配置信息
- `Format`：时间格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置RTC当前时间

### HAL_RTC_GetTime

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_GetTime(RTC_HandleTypeDef *hrtc, RTC_TimeTypeDef *sTime, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sTime`：指向RTC_TimeTypeDef结构体的指针，用于存储获取的时间
- `Format`：时间格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：获取成功
- `HAL_ERROR`：获取失败

**作用**：获取RTC当前时间

### HAL_RTC_SetDate

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_SetDate(RTC_HandleTypeDef *hrtc, RTC_DateTypeDef *sDate, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sDate`：指向RTC_DateTypeDef结构体的指针，包含日期配置信息
- `Format`：日期格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置RTC当前日期

### HAL_RTC_GetDate

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_GetDate(RTC_HandleTypeDef *hrtc, RTC_DateTypeDef *sDate, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sDate`：指向RTC_DateTypeDef结构体的指针，用于存储获取的日期
- `Format`：日期格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：获取成功
- `HAL_ERROR`：获取失败

**作用**：获取RTC当前日期

### HAL_RTC_SetAlarm

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_SetAlarm(RTC_HandleTypeDef *hrtc, RTC_AlarmTypeDef *sAlarm, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sAlarm`：指向RTC_AlarmTypeDef结构体的指针，包含报警配置信息
- `Format`：时间格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置RTC报警时间

### HAL_RTC_GetAlarm

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_GetAlarm(RTC_HandleTypeDef *hrtc, RTC_AlarmTypeDef *sAlarm, uint32_t Alarm, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sAlarm`：指向RTC_AlarmTypeDef结构体的指针，用于存储获取的报警信息
- `Alarm`：要获取的报警，可以是RTC_ALARM_A
- `Format`：时间格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：获取成功
- `HAL_ERROR`：获取失败

**作用**：获取RTC报警配置

### HAL_RTC_SetAlarm_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_SetAlarm_IT(RTC_HandleTypeDef *hrtc, RTC_AlarmTypeDef *sAlarm, uint32_t Format);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sAlarm`：指向RTC_AlarmTypeDef结构体的指针，包含报警配置信息
- `Format`：时间格式，可以是RTC_FORMAT_BIN或RTC_FORMAT_BCD

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置RTC报警时间并启用中断

### HAL_RTC_DeactivateAlarm

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_DeactivateAlarm(RTC_HandleTypeDef *hrtc, uint32_t Alarm);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `Alarm`：要禁用的报警，可以是RTC_ALARM_A

**返回值**：

- `HAL_OK`：禁用成功
- `HAL_ERROR`：禁用失败

**作用**：禁用RTC报警

### HAL_RTC_PollForAlarmAEvent

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_PollForAlarmAEvent(RTC_HandleTypeDef *hrtc, uint32_t Timeout);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：报警事件发生
- `HAL_TIMEOUT`：超时
- `HAL_ERROR`：错误

**作用**：轮询RTC报警A事件

### HAL_RTC_WaitForSynchro

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_WaitForSynchro(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：同步成功
- `HAL_TIMEOUT`：同步超时

**作用**：等待RTC寄存器同步

## 4. RTCEx操作函数

### HAL_RTCEx_SetTamper

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_SetTamper(RTC_HandleTypeDef *hrtc, RTC_TamperTypeDef *sTamper);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sTamper`：指向RTC_TamperTypeDef结构体的指针，包含防篡改配置信息

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置防篡改功能（不带中断）

### HAL_RTCEx_SetTamper_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_SetTamper_IT(RTC_HandleTypeDef *hrtc, RTC_TamperTypeDef *sTamper);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `sTamper`：指向RTC_TamperTypeDef结构体的指针，包含防篡改配置信息

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置防篡改功能并启用中断

### HAL_RTCEx_DeactivateTamper

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_DeactivateTamper(RTC_HandleTypeDef *hrtc, uint32_t Tamper);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `Tamper`：要禁用的防篡改引脚，可以是RTC_TAMPER_1

**返回值**：

- `HAL_OK`：禁用成功
- `HAL_ERROR`：禁用失败

**作用**：禁用防篡改功能

### HAL_RTCEx_PollForTamper1Event

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_PollForTamper1Event(RTC_HandleTypeDef *hrtc, uint32_t Timeout);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `Timeout`：超时时间

**返回值**：

- `HAL_OK`：防篡改事件发生
- `HAL_TIMEOUT`：超时
- `HAL_ERROR`：错误

**作用**：轮询防篡改1事件

### HAL_RTCEx_SetSecond_IT

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_SetSecond_IT(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：启用秒中断

### HAL_RTCEx_DeactivateSecond

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_DeactivateSecond(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：

- `HAL_OK`：禁用成功
- `HAL_ERROR`：禁用失败

**作用**：禁用秒中断

### HAL_RTCEx_BKUPWrite

**函数原型**：

```c
void HAL_RTCEx_BKUPWrite(RTC_HandleTypeDef *hrtc, uint32_t BackupRegister, uint32_t Data);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `BackupRegister`：备份寄存器编号，可以是RTC_BKP_DRx（x为1-42）
- `Data`：要写入的数据

**返回值**：无

**作用**：向指定的备份寄存器写入数据

### HAL_RTCEx_BKUPRead

**函数原型**：

```c
uint32_t HAL_RTCEx_BKUPRead(RTC_HandleTypeDef *hrtc, uint32_t BackupRegister);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `BackupRegister`：备份寄存器编号，可以是RTC_BKP_DRx（x为1-42）

**返回值**：

- 返回备份寄存器中的数据

**作用**：从指定的备份寄存器读取数据

### HAL_RTCEx_SetSmoothCalib

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTCEx_SetSmoothCalib(RTC_HandleTypeDef *hrtc, uint32_t SmoothCalibPeriod, uint32_t SmoothCalibPlusPulses, uint32_t SmouthCalibMinusPulsesValue);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `SmoothCalibPeriod`：平滑校准周期
- `SmoothCalibPlusPulses`：平滑校准正脉冲
- `SmouthCalibMinusPulsesValue`：平滑校准负脉冲值

**返回值**：

- `HAL_OK`：设置成功
- `HAL_ERROR`：设置失败

**作用**：设置RTC平滑校准

## 5. RTC 状态函数

### HAL_RTC_GetState

**函数原型**：

```c
HAL_RTCStateTypeDef HAL_RTC_GetState(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：

- 返回RTC状态，可以是：
  - `HAL_RTC_STATE_RESET`：RTC未初始化或禁用
  - `HAL_RTC_STATE_READY`：RTC已初始化并准备使用
  - `HAL_RTC_STATE_BUSY`：RTC进程正在进行
  - `HAL_RTC_STATE_TIMEOUT`：RTC超时状态
  - `HAL_RTC_STATE_ERROR`：RTC错误状态

**作用**：获取RTC外设状态

## 6. RTC 中断处理函数

### HAL_RTC_AlarmIRQHandler

**函数原型**：

```c
void HAL_RTC_AlarmIRQHandler(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理RTC报警中断请求

### HAL_RTCEx_TamperIRQHandler

**函数原型**：

```c
void HAL_RTCEx_TamperIRQHandler(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理防篡改中断请求

### HAL_RTCEx_RTCIRQHandler

**函数原型**：

```c
void HAL_RTCEx_RTCIRQHandler(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：处理RTC中断请求

## 7. RTC 回调函数

### HAL_RTC_AlarmAEventCallback

**函数原型**：

```c
void HAL_RTC_AlarmAEventCallback(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：RTC报警A事件回调函数，用户可重新定义此函数

### HAL_RTC_RegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_RegisterCallback(RTC_HandleTypeDef *hrtc, HAL_RTC_CallbackIDTypeDef CallbackID, pRTC_CallbackTypeDef pCallback);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `CallbackID`：回调ID，可以是：
  - `HAL_RTC_ALARM_A_EVENT_CB_ID`：RTC报警A事件回调ID
  - `HAL_RTC_TAMPER1_EVENT_CB_ID`：RTC防篡改1回调ID
  - `HAL_RTC_MSPINIT_CB_ID`：RTC MSP初始化回调ID
  - `HAL_RTC_MSPDEINIT_CB_ID`：RTC MSP反初始化回调ID
- `pCallback`：指向回调函数的指针

**返回值**：

- `HAL_OK`：注册成功
- `HAL_ERROR`：注册失败

**作用**：注册RTC回调函数（需要定义USE_HAL_RTC_REGISTER_CALLBACKS为1）

### HAL_RTC_UnRegisterCallback

**函数原型**：

```c
HAL_StatusTypeDef HAL_RTC_UnRegisterCallback(RTC_HandleTypeDef *hrtc, HAL_RTC_CallbackIDTypeDef CallbackID);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针
- `CallbackID`：要取消注册的回调ID

**返回值**：

- `HAL_OK`：取消注册成功
- `HAL_ERROR`：取消注册失败

**作用**：取消注册RTC回调函数（需要定义USE_HAL_RTC_REGISTER_CALLBACKS为1）

### HAL_RTCEx_Tamper1EventCallback

**函数原型**：

```c
void HAL_RTCEx_Tamper1EventCallback(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：防篡改1事件回调函数，用户可重新定义此函数

### HAL_RTCEx_RTCEventCallback

**函数原型**：

```c
void HAL_RTCEx_RTCEventCallback(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：RTC事件回调函数，用户可重新定义此函数

### HAL_RTCEx_RTCEventErrorCallback

**函数原型**：

```c
void HAL_RTCEx_RTCEventErrorCallback(RTC_HandleTypeDef *hrtc);
```

**参数**：

- `hrtc`：指向RTC_HandleTypeDef结构体的指针

**返回值**：无

**作用**：RTC事件错误回调函数，用户可重新定义此函数
