# 1.编译和打包镜像

```bash
./build.sh uboot  # 编译uboot
./build.sh kernel # 编译内核
./build.sh recovery # 编译Recovery
./build.sh buildroot # 编译文件系统
./build.sh firmware  # 打包启动系统所需镜像
./build.sh updateimg # 打包镜像  进入 rockdev 目录下可以看到打包的单独镜像。
```

# 2.烧写镜像

看镜像烧写手册15-17页

