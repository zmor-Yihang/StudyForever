# STM32F1xx HAL IWDG库函数说明文档

本文档介绍了STM32F1xx HAL库中的IWDG（独立看门狗）函数，包括函数原型、参数、返回值和具体作用。

## 1. IWDG __HAL 宏函数

### __HAL_IWDG_START

**宏原型**：

```c
#define __HAL_IWDG_START(__HANDLE__)                WRITE_REG((__HANDLE__)->Instance->KR, IWDG_KEY_ENABLE)
```

**参数**：

- `__HANDLE__`：IWDG句柄

**返回值**：无

**作用**：启用IWDG外设

### __HAL_IWDG_RELOAD_COUNTER

**宏原型**：

```c
#define __HAL_IWDG_RELOAD_COUNTER(__HANDLE__)       WRITE_REG((__HANDLE__)->Instance->KR, IWDG_KEY_RELOAD)
```

**参数**：

- `__HANDLE__`：IWDG句柄

**返回值**：无

**作用**：使用在重载寄存器中定义的值重载IWDG计数器（禁止写访问IWDG_PR和IWDG_RLR寄存器）

## 2. IWDG初始化

### HAL_IWDG_Init

**函数原型**：
```c
HAL_StatusTypeDef HAL_IWDG_Init(IWDG_HandleTypeDef *hiwdg);
```

**参数**：
- `hiwdg`：指向IWDG_HandleTypeDef结构体的指针，包含IWDG模块的配置信息

**返回值**：
- `HAL_OK`：初始化成功
- `HAL_ERROR`：初始化失败（句柄为NULL）
- `HAL_TIMEOUT`：等待寄存器更新超时

**作用**：根据IWDG_HandleTypeDef中指定的参数初始化IWDG**并启动看门狗**。在函数退出前，会刷新看门狗以确保正确的时间基准。不需要

## 3. IWDG操作函数

### HAL_IWDG_Refresh

**函数原型**：
```c
HAL_StatusTypeDef HAL_IWDG_Refresh(IWDG_HandleTypeDef *hiwdg);
```

**参数**：
- `hiwdg`：指向IWDG_HandleTypeDef结构体的指针，包含IWDG模块的配置信息

**返回值**：
- `HAL_OK`：刷新操作成功

**作用**：刷新IWDG，防止MCU复位。在正常操作期间，应用程序必须定期刷新IWDG计数器。 